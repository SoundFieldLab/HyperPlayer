# 每日构建（Nightly）—— 每天北京时间 21:00 从默认分支自动构建 Windows 测试版。
#
# 行为约定：
#   - 不 bump 正式版本号：仅在工作区拼 "<基础版本>-nightly.<YYYYMMDD>" 用于产物命名，不推回仓库。
#   - 固定使用 nightly 这个 tag/release，每次发布前覆盖上一版，仓库始终只保留一个当前 nightly。
#   - 自上次 nightly 构建以来的提交生成 changelog。
#   - **不写/不推 update.json** —— 不干扰「设置 → 检查更新」的正式版通道。
#   - 无新增提交时自动跳过（手动触发时可用 force=true 强制构建）。
#
# 需要仓库 Secrets：EVS_ACCOUNT_NAME / EVS_PASSWD（castLabs Widevine VMP 签名）。

name: Nightly Build 每日构建

on:
  schedule:
    # UTC 13:00 = 北京时间 21:00
    - cron: '0 13 * * *'
  workflow_dispatch:
    inputs:
      force:
        description: '强制构建（忽略「无新增提交」跳过逻辑）'
        required: false
        default: 'false'
        type: choice
        options: ['false', 'true']

concurrency:
  group: nightly-build
  cancel-in-progress: false

permissions:
  contents: write   # gh release create / 移动 nightly tag 需要

jobs:
  # ---------- 准备：日期 / nightly 版本 / changelog / 跳过判定 ----------
  prepare:
    name: 准备（日期 / 版本 / Changelog）
    runs-on: ubuntu-latest
    timeout-minutes: 10
    outputs:
      nightly_tag: ${{ steps.meta.outputs.nightly_tag }}
      nightly_version: ${{ steps.meta.outputs.nightly_version }}
      has_changes: ${{ steps.meta.outputs.has_changes }}
    steps:
      - name: 拉取代码（完整历史，供查找上一个 nightly tag 与生成 changelog）
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 安装 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: 计算日期 / nightly 版本 / changelog
        id: meta
        env:
          FORCE: ${{ github.event.inputs.force }}
        run: |
          DATE=$(TZ=Asia/Shanghai date +%Y%m%d)
          NIGHTLY_TAG="nightly"

          # 基础版本只读 package.json，不改仓库；nightly 版本仅用于产物命名
          BASE_VERSION=$(node -p "require('./package.json').version")
          NIGHTLY_VERSION="${BASE_VERSION}-nightly.${DATE}"

          # changelog 覆盖上一次 nightly 之后的提交；首次运行回退最近正式版 tag，再无则回退最近 50 条
          LAST_NIGHTLY=$(git tag -l 'nightly' --sort=-creatordate | head -1)
          LAST_STABLE=$(git tag -l 'v*' --sort=-creatordate | head -1)
          if [ -n "$LAST_NIGHTLY" ]; then
            RANGE="${LAST_NIGHTLY}..HEAD"
          else
            RANGE="${LAST_STABLE:+${LAST_STABLE}..HEAD}"
          fi

          # 无新增提交判定（仅当存在上一个 nightly tag 且非 force 时生效）
          HAS_CHANGES="true"
          if [ "$FORCE" != "true" ] && [ -n "$LAST_NIGHTLY" ]; then
            if [ "$(git rev-parse "$LAST_NIGHTLY")" = "$(git rev-parse HEAD)" ]; then
              HAS_CHANGES="false"
            fi
          fi

          {
            echo "# HyperPlayer Nightly ${DATE}"
            echo ""
            echo "**版本号：${NIGHTLY_VERSION}（基础 ${BASE_VERSION} + 构建日期，非正式发版，不自动升级版本号）**"
            echo ""
            if [ -n "$RANGE" ]; then
              echo "## 代码变动（自上次 Nightly 构建 ${RANGE%%..*} 起）"
              echo ""
              LOG=$(git log $RANGE --no-merges --pretty=format:"- %s (%h)" 2>/dev/null)
              if [ -z "$LOG" ]; then echo "- 无新增提交"; else echo "$LOG"; fi
            else
              echo "## 代码变动（暂无 nightly / 正式版 tag，最近 50 条提交）"
              echo ""
              git log -50 --no-merges --pretty=format:"- %s (%h)"
            fi
            echo ""
            echo ""
            echo "---"
            echo "⚠️ 自动构建的每日测试版本，可能不稳定。仅用于测试，不推送 update.json，不影响正式版检查更新。"
          } > changelog.md

          echo "nightly_tag=${NIGHTLY_TAG}" >> "$GITHUB_OUTPUT"
          echo "nightly_version=${NIGHTLY_VERSION}" >> "$GITHUB_OUTPUT"
          echo "has_changes=${HAS_CHANGES}" >> "$GITHUB_OUTPUT"

          echo "::group::Changelog 预览"
          cat changelog.md
          echo "::endgroup::"
          echo "已计算：tag=${NIGHTLY_TAG} version=${NIGHTLY_VERSION} has_changes=${HAS_CHANGES}"

      - name: 上传 changelog（供 release 作业读取）
        uses: actions/upload-artifact@v4
        with:
          name: nightly-meta
          path: changelog.md

  # ---------- Windows 安装包构建 ----------
  build-windows:
    name: Windows 安装包构建
    needs: prepare
    if: needs.prepare.outputs.has_changes == 'true'
    runs-on: windows-latest
    timeout-minutes: 60
    # EVS 凭据用于 Widevine VMP 签名（仅 Apple Music 原生 DRM 需要）。
    # 未配置 secrets 时自动降级为未签名构建，不阻断流水线。
    env:
      EVS_ACCOUNT_NAME: ${{ secrets.EVS_ACCOUNT_NAME }}
      EVS_PASSWD: ${{ secrets.EVS_PASSWD }}
      HAS_EVS: ${{ secrets.EVS_ACCOUNT_NAME != '' && secrets.EVS_PASSWD != '' }}
    steps:
      - name: 拉取代码
        uses: actions/checkout@v4

      - name: 安装 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      # --ignore-scripts：跳过第三方 postinstall（useragent-ng 的 ./bin/build.js 在 Windows cmd 下失败）。
      # Electron 二进制由下面单独的步骤显式下载。
      - name: 安装依赖
        run: npm ci --ignore-scripts

      - name: 写入 nightly 版本号到 package.json 与 lock（仅工作区，不推回仓库）
        env:
          NIGHTLY_VERSION: ${{ needs.prepare.outputs.nightly_version }}
        run: |
          node -e "const fs=require('fs');for(const f of ['./package.json','./package-lock.json']){const j=JSON.parse(fs.readFileSync(f));j.version=process.env.NIGHTLY_VERSION;fs.writeFileSync(f,JSON.stringify(j,null,2)+'\n')}"
          node -p "require('./package.json').version"

      - name: 下载 Electron 运行时（npm ci 跳过其安装脚本）
        run: node node_modules/electron/install.js

      - name: 安装 castLabs EVS（仅当配置了 EVS 凭据）
        if: env.HAS_EVS == 'true'
        run: py -3 -m pip install castlabs-evs==1.3.2

      - name: 构建安装包（已签名，Apple Music 原生 DRM 可用）
        if: env.HAS_EVS == 'true'
        env:
          EVS_NO_ASK: "1"
        run: npm run build:electron
        timeout-minutes: 40

      - name: 构建安装包（未签名：未配置 EVS 凭据，跳过 Widevine VMP）
        if: env.HAS_EVS != 'true'
        run: |
          Write-Host '::warning::未配置 EVS_ACCOUNT_NAME / EVS_PASSWD，本次为未签名构建；Apple Music 原生 DRM 不可用（会自动回退网易云/QQ 载体匹配），其余功能不受影响。'
          npm run build:electron:unsigned
        timeout-minutes: 40

      - name: 上传安装包
        uses: actions/upload-artifact@v4
        with:
          name: nightly-windows-installer
          path: release/*.exe

  # ---------- 覆盖旧 nightly + 发布固定 nightly 预发布 ----------
  release:
    name: 覆盖并发布固定 Nightly 预发布
    needs: [prepare, build-windows]
    if: ${{ needs.prepare.outputs.has_changes == 'true' && needs.build-windows.result == 'success' }}
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: 拉取代码（供 gh release 使用当前默认分支 HEAD）
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 下载 changelog
        uses: actions/download-artifact@v4
        with:
          name: nightly-meta

      - name: 下载 Windows 安装包
        uses: actions/download-artifact@v4
        with:
          name: nightly-windows-installer
          path: artifacts

      - name: 删除上一版固定 nightly（保留 tag，避免破坏变更检测）
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release delete nightly -y || echo "没有需要删除的固定 nightly release"
          git tag -f nightly "$GITHUB_SHA"
          git push origin refs/tags/nightly --force

      - name: 创建固定 nightly 预发布
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          ls -la artifacts
          gh release create nightly \
            --title "HyperPlayer Nightly" \
            --notes-file changelog.md \
            --prerelease \
            --target main \
            artifacts/*
          echo "已发布固定 nightly（预发布，不覆盖正式 latest）"
