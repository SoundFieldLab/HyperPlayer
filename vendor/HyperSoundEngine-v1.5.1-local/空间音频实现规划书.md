# 空间音频实现计划（修订 v2：折入全部缺口 + 覆盖矩阵）

> 本文保留为历史需求与性能目标输入。ADR-0003 已废止 TS 兄弟 Worklet 方案：TS 空间处理现为引擎内联第 22 级，Rust HRTF 核并入 Rust 支线；完整 1–22 级 wasm、Rust renderer、8 函数 ABI 与 Chromium E2E 已实现。本文 §3.2 契约、§八性能目标及数值对拍要求继续有效。真实 SOFA、Firefox 和物理 multichannel 仍待验收。

## 历史决策基线（已由 ADR-0003 取代）

选项 3（Rust 核 + TS 桥）｜全 4 模式｜Rust SOFA + 磁盘缓存，随包 KEMAR｜仅复用 HSE 文件｜**兄弟 Worklet 节点，HSE 核心零改动**（HyperSoundEngine/DSP/参数模型/worklet 不碰）。

## 历史架构（兄弟节点方案已作废）

```
masterGain → [soundtouch?] → engineNode → [spatialNode?] → analyser
TS UI(SpatialPage 四象限) → TS 控制(SpatialAudioController) → SpatialBackend
→ WasmHrtfBackend(Rust→spatial-worklet.js base64 内联) / TsConvolverBackend(参考,复用 Convolver)
```

## 本次修订：折入的缺口

- **SpatialPage 重构为四象限布局**（§5.1）：左面板(声源列表+布局预设)/中央(3D 视图)/左下(属性编辑：方位角/仰角/距离/增益/大小/衰减模型/参考距离/最大距离)/右下(房间环境+HRTF 配置)/状态栏。4 模式选择器并入顶部工具栏。
- **顶部工具栏**（§5.6）：播放控制 + 模式选择器(A/B/C/D) + 输出设备选择 + 采样率 + Buffer + 设置入口。
- **设置弹窗**（§5.6）：HRTF 数据集选择/导入 + 卷积模式(时域/频域) + HRIR 长度 + 最大对象数 + 性能模式(高质量/平衡/低延迟) + 快捷键 + 主题(深/浅)。
- **模式 B 补**：声源→扬声器路由 + 声源大小(点声源/扩散) + 底部仰角层(Bottom L/R) + 2D 环形辅助俯视图(可切换显隐)。
- **模式 C 补**：Tab 切换声源 + 声源运动轨迹关键帧编辑 + 半透明轨迹曲线显示。
- **接口契约**（§3.2）：Rust 核导出精确函数 `spatial_load_hrtf`/`spatial_get_hrir`/`spatial_render_objects`/`spatial_set_room`/`spatial_set_room_preset`/`spatial_set_hrtf_interp_mode`/`spatial_set_convolution_mode`/`spatial_set_distance_model`，TS 侧 `WasmHrtfBackend` 按此契约调。
- **性能目标显式采用**（§八）：<5ms 渲染延迟、32-64 对象(可配 128)、<25% 单核 CPU、HRTF ~50MB/套运行时 <200MB、44.1/48/96kHz、UI 60fps、**渲染循环零分配**。
- **多声道输入检测**（§模式 A，低优先）：自动检测立体声/多声道并显示，5.1/7.1 自动映射虚拟扬声器。
- **多声道输出 Multichannel**（§P4，标可选）：需真实多声道输出设备支持，Binaural/Stereo 先行，Multichannel 后补。

## Phase 0：工具链 + 接口骨架（1-2 周）

1. Rust 工具链（rustup/cargo/wasm-pack/wasm32 target）。
2. `rust/hrtf-core/` 骨架 + `wasm-bindgen`。
3. `src/spatial/SpatialBackend.ts` 接口（对齐 §3.2 契约函数）。
4. `src/spatial/TsConvolverBackend.ts`：复用 `dsp/Convolver.ts`，扩展立体声 IR 对 + 方向最近邻。
5. `src/spatial/SpatialProcessor.ts` 独立 worklet 处理器。
6. `scripts/build-spatial-worklet.mjs`（esbuild→`public/spatial-worklet.js`），加 `predev`/`prebuild`。
7. `attachEngine.ts` 加 `syncSpatialChain`（照抄 `syncPitchChain`，挂 engineNode 之后 analyser 之前；关时直连）。
8. `src/spatial/types.ts` `SpatialParams`（`mode:'off'` 默认），独立 localStorage。
9. 验证 worklet + 离线导出双路径 TS 参考后端跑通。

## Phase 1：Rust HRTF 核 + 数据集 + 契约（3-4 周）

1. Rust SOFA 解析（评估 soufi/libsofa 绑定或自写 NetCDF3）→ (az,elev)→HRIR 对网格。
2. 磁盘缓存（`os.tmpdir()`，带版本号失效）。
3. 随包 `resources/hrtf/mit-kemar.sofa`；预留 CIPIC/SADIE II 后续。
4. Rust 球谐插值 + 分区卷积(rustfft/realfft，对齐 TS Convolver 便对拍) + ITD/ILD + 距离衰减(3 模型) + 空气吸收。
5. **Rust 导出 §3.2 全部契约函数**（精确签名）。
6. wasm-pack build → .wasm；build-spatial-worklet.mjs base64 内联；构造器同步 `new WebAssembly.Instance(new WebAssembly.Module(bytes))`。
7. `WasmHrtfBackend.ts` 按契约调 WASM。
8. 数值对拍（TS 参考作 ground truth，容差 1e-6），通过后 TS 参考降级测试夹具。
9. 默认后端切 WasmHrtfBackend；延迟经 `getLatencySamples()` 上报。

## Phase 2：模式 A + B + 四象限布局 + 工具栏/设置弹窗（4-5 周，扩容）

1. `SpatialAudioController`（ListenerState + AudioObject[] + 每帧 §4.3 公式）。
2. **SpatialPage 重构四象限**（§5.1）：左面板(声源列表+布局预设)/中央 3D 视图/左下属性编辑(方位角/仰角/距离/增益/大小/衰减模型/参考距离/最大距离)/右下(房间环境+HRTF 配置)/状态栏。
3. **顶部工具栏**（§5.6）：播放控制+模式选择器(A/B/C/D)+输出设备+采样率+Buffer+设置入口。
4. **设置弹窗**（§5.6）：HRTF 数据集/导入 + 卷积模式(时域/频域) + HRIR 长度 + 最大对象数 + 性能模式 + 快捷键 + 主题。
5. 模式 A：立体声→2 虚拟扬声器(L=-30°,R=+30°)+HRTF+轻房间混响(复用 ReverbSimple)+距离；**多声道输入检测+5.1/7.1 映射**（低优先）；可调 Spread/空间强度/房间开关+预设；极简空间化动画+大开关+输入格式显示。
6. 模式 B：头锁定虚拟扬声器，预设 Stereo/5.1/7.1.4/自定义(≤16)，每扬声器方位角/仰角/距离独立，**+底部仰角层(Bottom L/R)**；**声源→扬声器路由**；**声源大小(点声源/扩散)**；球形编辑器(R3F 线框球面+发光点+拖拽/滚轮/双击/右键)+**2D 环形俯视图**(可切换显隐)。

## Phase 3：房间模拟 + 模式 C 世界漫游（4-5 周）

1. Rust 房间模拟：镜像源 1-3 阶 + FDN 晚期；7 预设。晚期尾巴可先复用 ReverbSimple，FDN 后补。
2. Rust 多普勒（§4.6 公式，时变变速）。
3. 模式 C：可移动听者(WASD+鼠标转视角+Q/E 升降)，每帧相对方位角/仰角/距离重算，声源拖拽；**Tab 切换声源**。
4. UI：Three.js 3D 世界(地面网格+房间边界+发光声源球+听者方向锥+距离虚线+小地图)；视角(透视/俯视/侧视/第一人称 F 切换)；R 重置；听者位置/朝向显示+移动速度滑块。

## Phase 4：模式 D + 轨迹关键帧 + Ambisonics + 多输出 + 打磨（4-5 周，扩容）

1. 模式 D：舞台/影院预设(音乐舞台/电影院/钢琴独奏/自然场景)，座位前/中/后，房间+混响/氛围滑块，替换/添加声源。
2. **声源运动轨迹关键帧编辑**（§7 P4 / §5.4）：声源沿时间轨迹运动 + 半透明轨迹曲线显示。
3. Rust Ambisonics 编解码(B-format FOA/HOA，环境声/混响 send)。
4. HRTF 个性化：KEMAR/CIPIC/**SADIE II** 切换 + 用户 SOFA 导入。
5. 性能：3D UI 实例化/LOD/视锥剔除；DSP 源优先级剔除；**渲染循环零分配**。
6. **多输出模式**：Binaural / Stereo 下混 / **Multichannel(可选，需多声道输出设备)**。
7. UI 打磨：HSE 主题(电光青→紫+噪点+GlassCard)+framer-motion+快捷键+状态栏(CPU/对象/延迟/采样率/HRTF/模式)。
8. 测试+文档：`src/spatial/test/` 数值对拍+模式切换+参数往返+四象限布局冒烟；更新 FUSION_GUIDE/HANDOVER/CONTEXT。

## 覆盖矩阵（历史规划书 → 历史实施计划，表示需求曾纳入计划，不代表当前实现或验收状态）

| 规划书章节        | 项                                                           | 计划位置     | 规划覆盖      |
| :---------------- | :----------------------------------------------------------- | :----------- | :------------ |
| §2 模式A          | 立体声→±30° / HRTF+混响+距离 / Spread+强度+房间 / 输入格式检测 / 多声道映射 | P2           | ✅             |
| §2 模式B          | 预设布局 / 每扬声器角度距离 / 添加删除≤16 / **声源路由** / **声源大小** / 顶部+底部仰角层 / 球形编辑器 / **2D 俯视** | P2           | ✅             |
| §2 模式C          | WASD+鼠标+Q/E / 拖拽声源 / 每帧重算 / 衰减+空气吸收+混响 / 视角切换 / 第一人称F / **Tab切换** / R重置 / 小地图 / **轨迹关键帧+曲线** | P3(+P4 轨迹) | ✅             |
| §2 模式D          | 4 预设场景 / 座位 / 房间+混响 / 替换添加声源                 | P4           | ✅             |
| §3.1 分层架构     | 四层                                                         | 全局         | ✅             |
| §3.2 接口契约     | 8 函数精确签名                                               | P1           | ✅             |
| §3.2 性能         | <3ms/64对象/零分配                                           | P1+P4        | ✅(采用为目标) |
| §4.1 HRTF         | SOFA/球谐/FFT分区/HRIR256/ITD-ILD / KEMAR+CIPIC+SADIE II     | P1+P4        | ✅             |
| §4.2 听者系统     | position/orientation/up_vector + 各模式行为                  | P2           | ✅             |
| §4.3 空间参数计算 | 公式                                                         | P2           | ✅             |
| §4.4 距离衰减     | 3 模型+空气吸收+直达/混响比                                  | P1           | ✅             |
| §4.5 房间模拟     | 镜像源1-3阶+FDN+7预设                                        | P3           | ✅             |
| §4.6 多普勒       | 公式(模式C)                                                  | P3           | ✅             |
| §4.7 遮挡/衍射    | 简化低通+障碍物UI+截止频率                                   | P3           | ✅             |
| §5.1 四象限布局   | 左面板/中央/左下/右下/状态栏                                 | P2           | ✅             |
| §5.2 模式A UI     | 动画+开关+3滑块+格式显示                                     | P2           | ✅             |
| §5.3 模式B UI     | 球形网格+发光点+拖拽/滚轮/双击/右键+2D俯视                   | P2           | ✅             |
| §5.4 模式C UI     | 3D场景+网格+边界+球体+锥体+虚线+轨迹+第一人称+小地图+视角+快捷键 | P3+P4        | ✅             |
| §5.5 模式D UI     | 3D缩略图+场景卡+座位+滑块                                    | P4           | ✅             |
| §5.6 工具栏       | 播放+模式选择+输出设备+采样率+Buffer+设置                    | P2           | ✅             |
| §5.6 状态栏       | CPU/对象/延迟/采样率/HRTF/模式                               | P4           | ✅             |
| §5.6 设置弹窗     | 数据集/卷积模式/HRIR长度/最大对象/性能模式/快捷键/主题       | P2           | ✅             |
| §5.7 技术选型     | Three.js/Canvas/framer-motion/状态管理                       | 全局         | ✅             |
| §6 集成           | 复合处理节点(兄弟节点)                                       | P0           | ✅             |
| §7 四 Phase       | 对齐                                                         | P0-P4        | ✅             |
| §7 P4 项          | Ambisonics/HRTF个性化/**轨迹关键帧**/UI打磨/性能/**多输出(含Multichannel)**/文档 | P4           | ✅             |
| §八 性能目标      | <5ms/32-64对象/<25%CPU/<200MB/96kHz/60fps/零分配             | P1+P4        | ✅             |
| §十 风险          | 桥接/头锁定感/HRTF不匹配/3D UI性能/WASD不直觉/FFT复杂        | 末段         | ✅             |

**空格=0**：矩阵无未覆盖项。

## 关键风险（不变 + 补）

| 风险                               | 对策                                                         |
| :--------------------------------- | :----------------------------------------------------------- |
| WASM-in-worklet base64 内联        | P0 先 TS 参考跑通管道再换；同步实例化不 await                |
| TS/Rust 数值不一致                 | 逐用例对拍 1e-6，TS 参考作测试夹具                           |
| Electron 打包漏 .wasm/破坏三大约束 | build.files 加 resources/hrtf/**；base:'./'/electronDist 不动；打包冒烟测黑屏 |
| 空间节点输出未经限幅               | HSE Limiter 保护其输入；空间节点内置 final limiter            |
| 模式 C WASD 不直觉                 | 引导教程+第一人称+小地图                                     |
| 通用 HRTF 前后混淆                 | 多套切换+用户 SOFA 导入                                      |
| 3D UI 多对象帧率                   | 实例化渲染+LOD+视锥剔除                                      |



## 总周期

约 16-21 周（较 v1 +2-2.5 周，因 P2/P4 扩容吸收四象限布局/工具栏/设置弹窗/轨迹关键帧/多输出/契约函数/SADIE II 等缺口）。每 Phase 有可验证里程碑。