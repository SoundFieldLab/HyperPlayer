# HyperPlayer 接线阶段交接提示词（2026-09-04）

> 供新会话执行「D34/D35 之后的接线」使用。删除轮已完成：Rust 收敛到曲库+壳、vendor/shared 四块代码已落位未接线、契约面已收敛。

## 你即将接手的工作

HyperPlayer 是一款现代化 Windows 桌面音乐播放器（Tauri 2 + React/TS + Rust），位于 `E:\FolderForVibeCoding\Zcode\HyperPlayer`。**2026-09-04 用户定调 D34/D35 对前后端边界做了彻底重定调**：播放、DSP、网易云协议、缓存治理全部迁入 WebView 前端（TypeScript）；Rust 只保留本地曲库（ADR-0004）+ 桌面壳 + 哑存储/转发桥。删除轮已把旧 Rust 播放链/DSP/网易云实现全部删除并保证编译与测试全绿。**你的任务是把已落位的 TS 代码接线成一条可播放的完整链路。**

**第一件事：精读三份文档（按顺序）**，它们是你全部工作的依据，冲突时以文档为准：
1. `AGENTS.md`（已按 D34/D35 重写，含硬性约束与「Rust 壳零领域知识」红线）
2. `docs/定调决策记录.md` 的 **D34（12 问）与 D35（8 问接缝）** 条目
3. `docs/需求基线.md`（「通信边界」与「待执行（顺序）」两节）
4. `docs/adr/0006-webview-playback-ts-services.md`

## 当前仓库状态（删除轮已交付，均已验证）

**Rust（已收敛，勿动边界）**：
- `crates/hyperplayer-engine`：只剩曲库（repository SQLite/lofty/扫描/封面），7 个测试全绿
- `src-tauri`：41 个 command 注册（library_* 25、settings 2、window 6、windows 集成 3、updater 3、bootstrap、log_web）、5 个事件（LIBRARY_SCAN_PROGRESS/SETTINGS_CHANGED/CLOSE_REQUESTED/MEDIA_KEY_PRESSED/UPDATER_STATUS_CHANGED）、34 个测试全绿、零警告
- `AppState.services` 只剩 `{ library, settings }`；`credential_vault.rs`（DPAPI 哑加密）**保留但未接线**（`#[allow(dead_code)]`，等待你补 2 个 command）
- `platform/windows/mod.rs` 的 SMTC 按钮已改为 emit `MEDIA_KEY_PRESSED`；`lifecycle.rs` 托盘也 emit 同事件（按钮名 play-pause/previous/next）
- `dto.rs` 468 行：SettingsDto.dsp 字段保留、PersistedDspConfig.configuration 已是 serde_json::Value
- `pnpm check:ipc` 通过（41 commands + 5 events 双向匹配）

**TS（已落位，均未接线）**：
- `vendor/netease-cloudmusic-api/`：`@neteasecloudmusicapienhanced/api` 4.39.0 完整源码（431 端点，MIT）。**三处手术待做**：`util/request.js` 的 axios → tauri-plugin-http fetch；`main.js`/`generateConfig.js` 的 fs/os.tmpdir 落盘 → 浏览器存储；`main.js` 的 fs 模块自动发现 → 构建期静态枚举。加密层（crypto-js/node-forge）是纯 JS，**不要动**
- `vendor/waveforge-netease/`：`local-server.mjs`（10,760 行，92 条网易云路由的业务逻辑：重试/缓存/音质降级/付费拦截/QR 透传）+ `services/musicApi.ts`（3,587 行，前端调用中枢+LRC/YRC 解析）等 5 个服务文件。**移植为 TS 服务层时以此为骨架**
- `vendor/waveforge-lyrics/`：6 个歌词渲染组件 + 解析三件套（ttmlParser/lyricWordTiming/lyricBoundaryParentheses）+ playbackTimeStore + useAudioPulse
- `shared/hypersoundengine/`：HSE v1.5.1 完整 TS（src 60 + ui 16 + test + specs 14MB，`link:shared/hypersoundengine` 已挂入根 package.json）。**含 `worklet.ts`（AudioWorklet 处理器）与 `browser.ts`（Web Audio 宿主），DSP 工作台用 `ui/HyperSoundEngineMixingStudio.tsx`**
- `app/` 前端 10.4k 行：UI 层（页面/组件/视觉体系）**原样保留**（D34 Q10：保 UI 层换服务层）；`app/bridge/` 已收敛到 41 command + 5 事件；`app/pages/`、`app/store.ts`、`app/player/`、`app/visualization/` **仍引用已删的播放/网易云/DSP bridge 函数——这是你的重写范围**（当前 14 个前端测试失败全部源于此，属预期）

**合规**：`vendor/README.md` 与 `THIRD_PARTY_NOTICES.md` 已登记（api-enhanced MIT、WaveForge 授权 https://github.com/SoundFieldLab/WaveForge 、folia/pv/unblock 排除声明）；`LICENSE-HSE-AUTHORIZATION.md` 覆盖 HSE TS 拷贝。新增 vendored 源码必须带原 LICENSE 并登记。

**animejs 4.5.0（2026-09-04 用户定调引入）**：已装为根依赖（MIT，Julian Garnier），notices 已登记。用途 = 前端动画库补充（复杂/编排式动画，如歌词逐字动效、封面氛围、播放器交互动效的精细编排）；**定位边界**：产品过渡与短反馈仍以 Motion + CSS 为主（UI 基线），animejs 是补充不是替代；**GSAP 仍永久排除**（D27）。包为 ESM/CJS 双格式（`node_modules/animejs/dist/modules/`），Vite 直接 `import anime from 'animejs'` 可用；v4 API 为模块化命名导出（`import { animate, createTimeline, stagger, createScope } from 'animejs'`），TypeScript 类型自带。接线时可按需封装成 `app/` 内的小工具层，不直接散落调用。

## 待执行任务（按顺序，每步保持编译/测试可验证）

### 1. vendored 包三处手术 + 网易云 TS 服务层
- 把 `vendor/netease-cloudmusic-api` 从 Node 向改成浏览器向（手术点见上），作为 `app` 可 import 的 TS 模块（建议在 `app/services/netease/` 下建协议适配层，不改 vendor 内文件；若必须改 vendor 文件，用 `HyperPlayer adaptations` 注释标记）
- 把 `vendor/waveforge-netease/local-server.mjs` 的路由业务逻辑（重试/缓存/音质降级/付费拦截/QR 登录透传）移植成 TS 服务层；**`song_url_match` 跨平台兜底与 unblock 相关路径禁止移植**（产品红线）
- 网络层全部走 `tauri-plugin-http`（**尚未安装**：`src-tauri/Cargo.toml` 加 `tauri-plugin-http = "2"`，capabilities 加 `http:default`；放开 HTTPS 任意域是 D35 Q19 定案，不做域名白名单）
- Cookie/会话：TS 内存持有；持久化经 DPAPI 保险库（补 `credential_get`/`credential_set` 两个 command 接 `credential_vault.rs`）
- 参考 `docs/音源-网易云-行为规范.md`（含 eapi 默认通道、weapi 被 WAF 拒需绕行、xeapi X25519、匿名 token 兜底等实测结论）

### 2. WebView 播放链 + HSE worklet
- 双源调度（D34 Q9）：本地完整数据（本地文件经 `asset:` 协议、OPFS 整轨缓存）→ `decodeAudioData` + AudioBufferSource 采样级 gapless；流式在线 → MediaElement + 预载快切
- `asset:` 协议：`src-tauri/tauri.conf.json` 的 app.security 加 assetProtocol scope（曲库目录）；CSP media-src 已有 asset: 允许
- HSE：用 esbuild 把 `shared/hypersoundengine/src/worklet.ts` 打成单文件 IIFE（`audioWorklet.addModule`），`browser.ts` 作宿主；`app/player/` 的播放服务接 HSE 图
- 多窗口（D35 Q18）：主窗口权威，播放状态（曲目/进度 1Hz/歌词行）经 `app.emit` 广播；桌面歌词/迷你播放器纯订阅

### 3. 前端服务层与分域 store
- 在 `app/` 建服务层：`neteaseService`（协议+业务）、`playbackService`（双源调度+HSE）、`cacheService`（OPFS 治理）、`dspService`（HSE 控制面）、`settingsService`（哑 KV command 封装）、`weatherService`（D35 Q20：天气 TS 完整实现，走 tauri-plugin-http）
- `app/store.ts`（684 行单 store）切分域 store：播放/曲库/网易云/DSP
- 修好 `app/pages/` 的引用（DspWorkbench 换成 HSE MixingStudio 容器 + 视觉统一：theme.ts 令牌、lucide→Phosphor；网易云页面接 neteaseService；SettingsView 接 settingsService；天气卡接 weatherService）
- bridge 保持 41 command 面，新服务不要新增 command（除上面已列的 credential 2 个与 SMTC 3 个）

### 4. 接缝接线（D35 落地）
- SMTC 上行：补 `smtc_update_metadata`/`smtc_update_playback_state`/`smtc_update_position` 3 个 command（TS 播放服务在曲目切换/状态变化/进度节拍推送；Rust 侧只写 SystemMediaTransportControls，零播放知识）
- 下行：前端订阅 `MEDIA_KEY_PRESSED`（按钮名 play/pause/stop/next/previous/play-pause），统一接到播放服务
- settings 哑 KV 已就绪；DSP 预设/HSE2 配置存 settings（SettingsDto.dsp 字段，serde_json::Value），fail-close 版本化迁移逻辑在 TS 实现
- 缓存治理（D23/D24/D30 规则 TS 重写）：VIP fail-closed 门禁（AccountEntitled 绑 userId+实时校验）、专辑 5 会话晋升、容量 10 GiB/90% trim/最近 100 曲目保护、Public 7 天离线证明；介质 OPFS（`navigator.storage.getDirectory()`），**数据跟随程序走**（WebView2 数据目录钉应用数据目录），estimate+记账双轨；「缓存不暴露为 MP3 文件」红线由 OPFS 物理满足

### 5. 验收
- `pnpm check:ipc`、`pnpm test`（修完 14 个旧测试或删除过时用例）、`cargo test`（crates/ 与 src-tauri/ 两处）、`pnpm frontend:build`
- 网易云：扫码登录 → 搜索 → 播放（VIP/普通账号矩阵）、付费拦截 fail-closed
- DSP 工作台：MixingStudio 在 Tauri/WebView2 实窗可用、参数实时生效
- SMTC/媒体键/托盘按钮全链路
- 汇报测试证据需区分测试替身与生产路径（用户要求）

## 红线（违反即返工）
- **消灭平行实现**：网易云与 DSP 各只允许一份实现（vendored 包 / HSE 完整 TS）；不得复活或新造 Rust 播放链/DSP/网易云
- **Rust 壳零领域知识**：新 command 出现协议/DSP/播放/Cookie 语义即违规；Rust 只许曲库、哑存储、纯转发、系统集成
- folia（AGPL）/pv/unblockneteasemusic/跨平台匹配**永不引入**
- 不做 Unblock、替代源解灰、伪造 VIP；没有官方有效 URL 时停止
- 不提供音乐文件下载/导出；缓存应用私有
- vendored/shared 源码改动标注、不回改上游；HSE UI 只做视觉统一不大改结构
- **animejs 是补充不是替代**：产品过渡与短反馈仍以 Motion + CSS 为主；**GSAP 仍永久排除**（D27）
- 文档与注释用中文；正式 UI 只在 Tauri/WebView2 验收，浏览器渲染不作通过证据
