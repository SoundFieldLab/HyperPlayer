# HyperPlayer 功能清单 3.0

> 生成日期：2026-09-11　｜　版本 `1.0.0`　｜　分支 `main`
> 依据：**代码实测**（grep / ls / 读文件）。每条「关键实现」均为仓库内真实存在的路径；与源码冲突时以源码为准。
> 覆盖 **12 个功能域**（A–L）+ 统计 + 附录 A（红线文件）/ B（已知限制）/ C（文档索引）/ D（存疑复核）。
>
> 命名说明：本项目由 WaveForge 减配并改名而来。音效引擎品牌为 **HSE**，代码标识符仍为
> `v3` / `EngineV3` / 目录 `src/services/HyperSoundEngine-v1/`（未改名）。

---

## 域索引

**A** 应用框架与视图模式 ｜ **B** 音乐平台与账号 ｜ **C** 搜索与发现 ｜ **D** 播放核心 ｜
**E** 歌词系统 ｜ **F** 音效 HSE ｜ **G** 可视化 ｜ **H** 内容消费 ｜ **I** 社交与个人中心 ｜
**J** 桌面模式与系统集成 ｜ **K** 插件系统 ｜ **L** 后端与工程

---

## 域 A：应用框架与视图模式

| 功能 | 说明 | 关键实现（file:line 或路径） |
|---|---|---|
| 四视图框架 | 同一套 React 应用在 4 种界面形态间切换，`App.tsx` 按 `ViewMode` 懒加载对应视图，共享播放/队列/设置等运行时状态 | `src/types/playbackNavigation.ts:3`（`ViewMode = 'explore' \| 'minimal' \| 'traditional' \| 'desktop'`）、`src/App.tsx:61-69`（懒加载）、`src/App.tsx:6444` |
| 简约模式 minimal | 三栏布局 + 全屏沉浸播放页，是整软件的「总设置」宿主，功能最完整 | `src/components/HomeView.tsx`、`src/components/SettingsPanel.tsx` |
| 传统模式 traditional | 经典音乐软件布局（左歌单/中列表/右歌词），内置传统搜索、歌单/专辑/歌手详情、评论等子页 | `src/components/TraditionalView.tsx`、`TraditionalSearch.tsx`、`TraditionalPlaylistDetail.tsx`、`TraditionalComments.tsx` |
| 探索模式 explore | 以推荐/发现为中心的新式首页，按平台渲染不同区块，含网/QQ 专属探索页 | `src/components/ExploreView.tsx`、`src/features/neteaseExplore/NeteaseExplorePage.tsx`、`src/features/qqExplore/QQExplorePage.tsx` |
| 桌面模式 desktop | 壁纸 + 可拖拽左右两列小组件 + 窗口融合穿透的桌面化形态 | `src/components/DesktopView.tsx`、`DesktopCustomizationEditor.tsx` |
| 模式切换 | 统一切换入口（HomeView 顶部「打开模式选择」→ 卡片面板），经 `viewModeChanged` 事件与 `localStorage.viewMode` 落地 | `src/components/ModeSelectionPanel.tsx:82`、`src/components/ModeSelectionCards.tsx`、`src/App.tsx:2500-2590` |
| 模式切换过渡动画 | 切换时全屏覆盖动画（3s 最短、12s 兜底）掩盖新模式挂载卡顿；16 粒子 + 模式文案 | `src/components/ModeTransitionOverlay.tsx:31`、`src/App.tsx:497-498,6309-6310` |
| 模式显示/隐藏自定义 | 用户可在模式面板勾选显示哪些模式，至少保留简约模式 | `src/components/ModeSelectionPanel.tsx:16-65`（`hyperplayer_visible_modes`）、`ModeSelectionPanel.tsx:208` |
| 设置中心（4 标签页） | 账号 / 个性化 / 高级 / 关于，简约模式设置是全局总设置 | `src/components/SettingsPanel.tsx:173`（`activeTab`）、`SettingsPanel.tsx:1204-1300` |
| 设置镜像机制 | 简约模式新增的全局设置，经声明式注册表自动镜像到传统/探索/桌面的设置页，任意一端改动全软件同步 | `src/services/globalSettingsRegistry.ts`（8 组 / 46 条）、`src/components/MirroredGlobalSettings.tsx` |
| 深色/浅色主题 | 全局主题，播放页快捷设置与设置页实时同步 | `src/services/globalSettingsRegistry.ts:338-352`（`playerTheme`） |
| 主题色（8 预设） | 天空蓝/翡翠绿/紫罗兰/玫瑰红/橙黄/珊瑚红/青色/石板灰，默认 `#3B82F6` | `src/components/SettingsPanel.tsx:818-828`、`SettingsPanel.tsx:2294` |
| 歌词字体选择器 | 内置推荐字体 + 本机字体下拉选择（镜像 UI 专用 `font-picker` 控件） | `src/components/FontPicker.tsx`、`src/services/globalSettingsRegistry.ts:59-60` |
| 播放快捷键设置 | 播放页快捷键开关、空格播放暂停、快进/快退秒数（1–15s，默认 5s）、多媒体键开关 | `src/services/playbackShortcutSettings.ts:1-56`、`src/components/SettingsPanel.tsx:1552` |
| 全局媒体键 | 注册 MediaPlayPause / MediaNextTrack / MediaPreviousTrack 三个全局快捷键，可整体启用/注销 | `desktop/main.cjs:710-724`、`desktop/main.cjs:7092` |
| 播放径向菜单 | 长按歌曲呼出方向式径向菜单，支持滚轮选择；动作可自定义 | `src/components/PlaybackRadialMenu.tsx:29-57`、`src/components/PlaybackRadialWheel.tsx` |
| 径向菜单动作自定义 | 9 种可选动作（再听/收藏/评论/专辑/歌手/详情/加歌单/复制信息/相似歌曲），按平台能力过滤，最多启用 8 个 | `src/services/playbackRadialMenuSettings.ts:17,36-45`、`src/components/PlaybackRadialMenuCustomizeModal.tsx` |
| 标题栏窗口控制 | 自绘标题栏，提供最小化/最大化还原/关闭，含 Windows kiosk 全屏路径处理 | `src/components/TitleBar.tsx:7-76` |
| 法律声明与用户协议 | 简体中文单语全文，**17 章**（总则 / 第三方平台风险 / 账号凭证 / 行为规范 / 知识产权 / 免责声明 / 责任限制 / 隐私 / 开源分发 / 协议变更 / 争议解决 / 设备授权 / 商标 / 未成年人 / 服务中止 / 权利通知 / 一般条款）。入口在**设置 → 关于 → 法律声明/用户协议** | 条款 `src/components/legal/LegalAgreement.tsx:30,126`；入口与弹窗 `src/components/SettingsPanel.tsx`（`showLegalModal`）；引导文案 `PlatformLoginNotice.tsx:105` |
| 平台登录使用须知 | 首次登录任意平台后弹一次，提示第三方客户端调用非官方接口的违约风险 | `src/components/PlatformLoginNotice.tsx:5,97`、`src/App.tsx:6132` |
| 灰色歌曲跨平台补全免责声明 | 开启该功能前必须阅读免责声明并等待倒计时结束 | `src/components/SettingsPanel.tsx:557-572,3406` |
| 更新系统 | 启动自动检测、检查新版本、版本历史、跳过版本；详情/下载/重启由全局 UpdateManager 承接 | `src/components/UpdateManager.tsx`、`desktop/update-manager.cjs`、`src/services/versionHistory.ts`、`src/components/SettingsPanel.tsx:3140` |

---

## 域 B：音乐平台与账号

| 功能 | 说明 | 关键实现（file:line 或路径） |
|---|---|---|
| 平台抽象层 | `MusicPlatform` 联合类型与 `MUSIC_PLATFORMS` 常量集中定义 4 个平台，UI 按能力注册表增减功能 | `src/services/platforms.ts:12,14` |
| 网易云音乐 | 后端 92 条 `/api/netease/*` 路由，进程内集成增强版 API；支持搜索/播放/歌单/收藏/评论/云盘/排行等最全能力 | `local-server.mjs`（`grep -oE "'/api/netease/...'"` 92 条）、`src/services/musicApi.ts` |
| QQ 音乐 | 后端 63 条 `/api/qq/*` 路由，QRC 歌词解密 + zzcSign 签名；支持歌单/收藏/评论/MV/艺人/关注等 | `local-server.mjs`（63 条）、`server/qrc-decoder.mjs` |
| Apple Music | 官方 Web API 代理（3 条 `/api/apple/*`：amp / license / rss）+ 播放面 Python bridge；支持 CENC HLS 原生播放、资料库、编辑精选 | `local-server.mjs`（3 条）、`server/apple-artwork-api.mjs`、`python-apple-bridge/apple_bridge.py`（端口 18790）、`src/services/apple*.ts` |
| Spotify | 纯前端 OAuth + Web API 客户端（无本地后端代理路由，直接 `api.spotify.com`）；搜索/歌单/曲目/发行/分类；官方流受 DRM 保护，播放走网易云/QQ 载体 | `src/services/spotifyService.ts:12-341`、`src/services/platforms.ts:191-218`（`playAsCarrier:false`） |
| B站看歌（独立功能） | **非平台成员**，独立功能。26 条 `/api/bilibili/*` 路由：WBI 签名搜索、DASH 音画分离、CC 字幕当歌词、弹幕/评论/点赞/投币/收藏、个人主页、扫码登录 | `src/services/bilibiliApi.ts:21`、`server/bilibili-api.mjs`、`src/components/BilibiliMvPlayer.tsx` |
| 平台能力矩阵 | 每平台声明 36 项能力（登录/歌单/收藏/搜索/歌词/评论/日推/榜单/MV/云盘/电台/音质等），UI 据此显隐 | `src/services/platforms.ts:57-245`（`PlatformCapabilities` 36 字段、`PLATFORM_CAPABILITIES`） |
| 网易云登录 | 扫码登录（`/api/netease/login/qr/key` + `/create` + `/check`） | `src/components/LoginPanel.tsx:86-105` |
| QQ 登录 | 网页扫码登录（`/api/qq/login/qr/check`），另有 cookie 手动填写路径 | `src/components/LoginPanel.tsx:50-51`、`src/components/QQLoginPanel.tsx` |
| Apple 登录 | Developer Token + Media-User-Token；Electron 内置窗口一键登录 Apple ID 抓取凭据，或手动填写 Token | `src/components/AppleLoginPanel.tsx:29,61,140`、`src/services/appleAuth.ts` |
| Spotify 登录 | 官方 OAuth 授权弹窗 + 自定义 Client ID（PKCE 流程，`spotify_client_id`） | `src/components/SpotifyLoginPanel.tsx:12,48,138-158`、`src/services/spotifyService.ts:46` |
| B站登录 | 扫码登录 + 登录态本地保存 | `src/components/BilibiliLoginPanel.tsx`、`src/services/bilibiliApi.ts:318-348` |
| 登录态过期管理 | 记录各平台登录有效期，探测过期并提示剩余天数 | `src/services/loginExpiry.ts:16-94` |
| 平台 cookie 单事实源 | QQ cookie 只经显式登录接口写全局，播放/读取路由用 `resolveRequestCookie` 只读不回写 | `local-server.mjs`（路由约定）、`src/services/platforms.ts:349-357`（`getPlatformCookie`） |
| QQ API Key 引导窗 | 独立 session partition（`hyperplayer-qq-skill-key`，每次打开前清空）直达 y.qq.com 领取 `qmk-` API Key，自动检测/引导复制 | `desktop/main.cjs:5302-5306,5606-5678`、`src/services/qqMusicSkills.ts:47` |
| 平台顺序自定义 | 用户可自定义平台显示顺序，三模式继承 | `src/services/platforms.ts:270-295`（`hyperplayer:platformOrder`） |
| 平台可见性（隐藏） | 可隐藏平台，至少保留一个可见平台 | `src/services/platforms.ts:304-344`（`hyperplayer:hiddenPlatforms`） |
| 平台跨视图同步 | 全局平台键 + 自定义事件，各视图读同一份选中平台 | `src/services/platformSync.ts:4-31` |
| 账号信息 / 个人中心 | 用户资料、关注/粉丝、动态、听歌排行、收藏、消息；查看他人主页导航栈 | `src/components/ProfileView.tsx:518-545` |
| 平台收藏语义 | 各平台「我喜欢 / 喜爱歌曲 / 音乐库」用户可见名称统一映射 | `src/services/platforms.ts:252-265` |

---

## 域 C：搜索与发现

| 功能 | 说明 | 关键实现（file:line 或路径） |
|---|---|---|
| 综合搜索 | 按类型搜索（单曲/歌手/专辑/歌单），含跨平台模式与融合模式；结果内联详情面板 | `src/components/SearchPanel.tsx:251-758` |
| 融合搜索 Fusion | 按意图（歌手/专辑/单曲/混合）加权融合多平台结果，权益去重排序 | `src/services/fusedSearch.ts:5,104-297`、`src/components/SearchPanel.tsx:55,584-627` |
| 搜索联想 | QQ smartbox 结构化联想（歌手/歌曲/专辑）；网易云联想 | `src/components/SearchPanel.tsx:151-168` |
| 搜索热词 | 拉取平台热搜词；Apple 无热词接口 | `src/components/SearchPanel.tsx:365-379` |
| 搜索历史 | 按平台分别持久化（网易云/QQ/Apple/Spotify/融合各一键） | `src/components/SearchPanel.tsx:45-50` |
| 首页模块化推荐 | **20 种**模块自选，最多 **6 个**（`MAX_HOME_MODULES=6`），按平台过滤 + 登录态给默认组合 | `src/services/homeModules.ts:2-23,34,36-57`、`src/components/HomeCustomizeModal.tsx` |
| 探索模式区块 | 7 种区块（为你发现/音乐旅程/推荐歌单/排行榜速览/最新音乐/新碟上架/声音与频道），按平台能力增减与排序 | `src/components/ExploreSettingsPanel.tsx:20,62-70`、`src/services/platforms.ts:48` |
| 私人 FM | 网易云连续个性化电台，支持「不再播放（垃圾桶）」负反馈 | `src/services/musicApi.ts:3366-3370`、`homeModules.ts:38` |
| 每日推荐 | 网易云账号每日歌曲推荐；QQ「每日 30 首」（不可用时猜你喜欢补齐） | `src/services/musicApi.ts:3384-3388`、`homeModules.ts:37,45` |
| 原生推荐协议 | 网易云 **12** 条 `/api/netease/native/*` + QQ **11** 条 `/api/explore/qq/native/*` 原生 feed（首页卡片流/雷达/日推/播客/漫游等） | `server/netease-native-explore.mjs`（12 条）、`local-server.mjs:6852-7048`（11 条） |
| 榜单 | 热歌榜/飙升榜/新歌榜等，网易云/QQ/Apple/Spotify 均有榜单能力 | `src/services/musicApi.ts`、`src/services/spotifyService.ts:274`（`fetchSpotifyCharts`） |
| 电台 / 声音频道 | DJ 电台与播客/声音频道（网易云 `dj/*`、原生播客路由） | `server/netease-native-explore.mjs`（`daily-podcast`/`podcast-home`）、`src/components/ExploreView.tsx` |
| 音乐旅程 | 网易云/QQ 听歌报告（排行/报告/偏好/归档） | `src/components/NeteaseMusicJourney.tsx:205`、`src/components/QQMusicJourney.tsx:126`、`src/services/neteaseMusicJourney.ts:31` |
| 相似歌曲 / 猜你喜欢 | 相似歌曲、听相似、看相似；QQ 99 号个性化电台 | `src/services/musicApi.ts`（`song/similar`、`song/listen-also`）、`homeModules.ts:44` |
| AI 推荐歌单 | QQ 音乐官方 Skills 个性化歌单，需 `qmk-` Key | `homeModules.ts:46`（`qq_ai_playlists`, `officialSkill:true`）、`src/services/qqMusicSkills.ts` |
| Apple 探索 | 复刻 music.apple.com 主页/新发现/广播/资料库 | `src/components/AppleExplorePanel.tsx`、`src/services/appleExploreService.ts` |
| Spotify 探索 | 官方 API 新发行/编辑精选/分类/榜单 | `src/services/spotifyService.ts:234-274` |

---

## 域 D：播放核心

| 功能 | 说明 | 关键实现（file:line 或路径） |
|---|---|---|
| 播放引擎 | 双 deck（两个 HTMLAudioElement）经 Web Audio 图接入增益/分析链，支持 WebView2 播放面对接 | `src/hooks/useAudioPlayer.ts:390-391`（双 deck 接线）、`src/audio/playbackTimeStore.ts` |
| 播放队列 | 队列重构、确定性下一首索引、预取上下文，支持顺序/随机/单曲循环 | `src/audio/PlaybackQueue.ts:34-53`、`src/App.tsx:592`（`playMode`） |
| 播放模式 | 顺序播放 / 随机播放（seeded shuffle）/ 单曲循环三种 | `src/App.tsx:4573-4583`、`src/audio/types.ts:1` |
| Fixed Crossfade（唯一智能过渡） | 等功率增益曲线交叉淡化，时长 1–12s 可调（默认 4s）；过渡计划层只产出 `fixed-crossfade` | `src/audio/transitionPlanner.ts:7-21,251-361`、`src/audio/TransitionRenderer.ts` |
| Gapless 无缝衔接 | 两种方案：已缓存就绪时「直接拼接」（头尾不掐），否则「60ms 等功率淡入淡出」消除数字硬切爆音；含预热/静音预启动/ended 拼接/兜底 | `src/services/gapless/seamlessJoinController.ts:66,222-238`、`src/services/gapless/gaplessTransition.ts:6-28`、`src/services/gapless/gaplessConstants.ts` |
| 专辑无缝 albumGapless | 同专辑相邻曲目尾部静音检测 + Equal Power 融合，可独立开关（默认开） | `src/services/albumGapless.ts:99,203-217`、`src/services/globalSettingsRegistry.ts:403` |
| 切歌方案提示 | 切歌后右上角提示本次衔接方式（直接拼接 / 60ms 淡入淡出 / albumGapless 交叉淡化 / 交叉淡化），仅开发者过渡调试开启时显示 | `src/components/GaplessModeToast.tsx:13`、`src/App.tsx:1932-1975` |
| 过渡模式互斥 | Crossfade 与 Gapless 互斥（开一个自动关另一个） | `src/App.tsx:4437-4446`、`src/services/globalSettingsRegistry.ts:374-410` |
| 音质设置 | 网易云/QQ/Spotify 6 档（自动/标准/高/极高/无损/Hi-Res）；Apple 5 档（自动/AAC/无损/Hi-Res 无损/Atmos，后三项当前设备未支持时禁用） | `src/services/audioQualitySettings.ts:3-16,35-51`、`src/components/AudioQualitySettingsModal.tsx:31-63` |
| 音质按平台持久化 | 每平台独立音质偏好，作用于播放、下一首预加载与链接缓存 | `src/services/audioQualitySettings.ts:18-25`、`src/components/AudioQualitySettingsModal.tsx:148` |
| 输出设备切换 | 枚举输出设备并用 `AudioContext.setSinkId` 切换，支持热插拔刷新与记忆 | `src/services/audioOutput.ts:98-182`、`src/components/PlaybackDeviceModal.tsx:91` |
| 变速变调 | 独立变调（±10 半音）与倍速（0.25–3x），实时复用 SoundTouch AudioWorklet（LGPL），串接于 masterGain → SoundTouch → v3 节点之间 | `src/services/HyperSoundEngine-v1/attachV3Engine.ts:21-24,160-211`、`src/services/HyperSoundEngine-v1/ui/modalsDynamics.tsx:287-298` |
| 下一首预载 | 结束前提前预热下一首，含元数据校验与就绪清理，保证 gapless/交叉淡化成功率 | `src/hooks/useAudioPlayer.ts:1563-1682` |
| 后台预取 | 空闲时预取探索首页数据（`requestIdleCallback`） | `src/services/backgroundPrefetch.ts:111,180-191` |
| 缓存体系 | IndexedDB 4 store（covers/playlists/lyrics/metadata）+ 封面 LRU 上限；服务端字节 LRU | `src/services/indexedDBCache.ts:2-7,76-82`、`src/services/cacheManager.ts:152-183`、`server/byte-lru-cache.mjs` |
| 分析用音频下载缓存 | 主进程下载音轨到本地缓存供 MV 对齐/离线渲染复用（**非用户可见的「无损下载」功能**） | `desktop/audio-download.cjs:1-6,239-242`、`desktop/main.cjs:6770-6879` |
| MediaSession | 向系统注册媒体元数据与播放/暂停/上一首/下一首动作处理器 | `src/App.tsx:4917-4979` |
| 任务栏播控 | 缩略图按钮（thumbar）+ 任务栏播放进度条 | `desktop/main.cjs:1648-1757` |
| 灰色歌曲跨平台补全 | 平台无版权/无播放链接时从其他公开源跨平台匹配同曲播放（离线兜底） | `src/components/SettingsPanel.tsx:2487-2489`、`src/App.tsx`（播放 fallback 逻辑） |

> **纠正旧清单说法**：上一版清单曾列「无损音频下载（desktop/audio-download.cjs）」。实测该模块是**分析/渲染用的音频缓存下载**（注释即 `downloading audio ... for analysis with persistent cache`），渲染层唯一相关入口 `saveWav` 在 `src/` 中无调用方（仅类型声明 `src/electron.d.ts:610`）。**当前未发现用户可见的「无损下载」功能**；用户可导出音频的入口只有 HSE 调音室的「导出 MP3」。

---

## 域 E：歌词系统

| 功能 | 说明 | 关键实现（file:line 或路径） |
|---|---|---|
| 歌词解析层 | 支持 LRC / YRC 逐字 / Apple TTML / 翻译 / 罗马音 / 对唱（ttm:agent） | `src/utils/ttmlParser.ts:7-61,216-328`、`src/utils/lyricWordTiming.ts` |
| 逐字时间轴工具 | 字形切分、逐字时间归一化、真实逐字判定、渐进填充（无逐字时按行时长推算） | `src/utils/lyricWordTiming.ts:4-188` |
| 多歌词源 | 平台官方源（网易云/QQ）+ AMLL TTML DB（多镜像回退）+ Apple Music（登录后）+ Lrclib 第三方源，流式先到先显 + 后续增强 | `src/services/musicApi.ts:1227-1430`（多源并发 `getLyrics`）、`musicApi.ts:1855-1926`（AMLL/Lrclib） |
| 自适应最佳歌词 + 同步性校验 | 选逐字质量最高骨架，再按时间补翻译/罗马音；第三方/Apple 源须通过时长一致性与平台时间轴交叉验证，否则降权 | `src/services/musicApi.ts:1415-1430` |
| 歌词来源策略设置 | 第三方歌词源开关、自适应最佳、首要歌词库（AMLL / Apple Music / 网易云 / QQ / 当前平台）、启用 Apple Music 歌词 | `src/services/globalSettingsRegistry.ts:443-560` |
| 现代模式 modern | 默认逐字卡拉OK；三档特效 clear（清晰）/ soft（柔和）/ apple（Apple 逆向还原，词整体渐亮）；弹簧滚动（`amodern`）与原生居中两种切换风格；间奏动画、背景和声 | `src/components/LyricsDisplay.tsx:58,87,546-655` |
| 沉浸模式 immersive | 全屏播放页 + 可自动隐藏的悬浮控制条（含翻译/罗马音按钮） | `src/components/ImmersiveControls.tsx:10-119`、`src/App.tsx` |
| 看歌模式 video | B 站 MV 播放 + 字幕当歌词 + 音画对齐（`mvAlignment`）+ 弹幕层；歌词进度随 MV | `src/components/BilibiliMvPlayer.tsx`、`src/components/BilibiliMvBackground.tsx`、`src/services/mvAlignment.ts:33-508`、`src/App.tsx:961` |
| PV 模式 pv | 全自动视觉编排：**30 个模板**（templates 数组）+ **78 个 PixiJS 特效**（registry 注册数），按节拍/情绪/段落切换模板 | `src/components/pvLyrics/PvLyricsPage.tsx:19,201-283`、`src/vendor/pv/templates/index.ts`、`src/vendor/pv/effects/index.ts`、`src/vendor/pv/core/engine.ts` |
| 歌词模式可见性/排序 | 用户可隐藏/排序 4 种歌词模式卡片（首次升级自动补入 video/pv） | `src/App.tsx:292-337,305`、`src/App.tsx:885-886` |
| 桌面歌词独立窗 | 独立透明窗口（`desktop-lyrics.html` 入口 + 主进程 IPC 持久化），支持横排/竖排、单/双行、字号、字体、颜色（自动/静态色）、锁定穿透 | `src/desktop-lyrics/DesktopLyricsApp.tsx:26-27,425-488`、`desktop/main.cjs:770-1034`、`desktop/desktop-lyrics-preload.cjs` |
| 桌面歌词翻译/罗马音/繁简 | 独立窗可开关翻译、罗马音、显示繁体（繁简转换），双行时副行取翻译/罗马音/下一句 | `src/desktop-lyrics/DesktopLyricsApp.tsx:386-394,456-488` |
| 翻译浮层 | 播放页翻译可选「传统（歌词下方）」或「现代（右下角浮动）」，位置可配 | `src/components/TranslationDisplay.tsx:4-10`、`src/App.tsx:850-852,7284`、`src/services/globalSettingsRegistry.ts:461-490` |
| 日语振假名注音 | 解析 `汉字（假名）` 形式并用 `<ruby>/<rt>` 渲染注音，逐字模式下按字形单元对齐 | `src/components/LyricsDisplay.tsx:1314-1360,1439-1546` |
| 对唱着色 | Apple TTML 的 `ttm:agent` 按演唱者分色（主唱/对唱/背景和声） | `src/services/appleMusic.ts:36-61,380-447`、`src/utils/ttmlParser.ts:222-278` |
| 歌词同步时钟 | 30fps 平滑时钟仅在有逐字/间奏动画/背景和声消费者时运行，空闲自动停转 | `src/components/LyricsDisplay.tsx:686-731` |

---

## 域 F：音效 HSE（HyperSoundEngine，代码名 v3）

> 模块：`src/services/HyperSoundEngine-v1/`，**当前唯一音效引擎**。许可证 CC BY-NC-ND 4.0 + 作者专项授权（红线：模块内 LICENSE / 授权补充说明 / THIRD_PARTY_NOTICES 不可删改）。

| 功能 | 说明 | 关键实现（file:line 或路径） |
|---|---|---|
| 处理链（14 级主链 + 第 15 级空间音频） | 顺序固定：输入 → 响度归一化增益 → 3D 环绕 → M/S(width+voiceBalance) → Pre-EQ → Deesser → Compressor → NightMode → 混响 → BassEnhancer → LoudnessComp → IEQ(Post) → [分析取样/LUFS 采样] → Limiter → 空间音频 → 输出 | `src/services/HyperSoundEngine-v1/src/engine/EngineV3.ts:486-660`（各级注释 1–15） |
| 渲染模式 | AudioWorklet 优先、失败自动回退 ScriptProcessor（`mode:'auto'`），worklet 单文件由脚本打包 | `src/services/HyperSoundEngine-v1/attachV3Engine.ts:263,293`、`scripts/build-v3-worklet.mjs`、`public/v3-worklet.js` |
| 音效项（12） | 混响 / 3D 环绕 / 低音增强 / 动态压缩 / 夜间模式 / 齿音抑制 / 智能均衡 / 限幅器 / 变速变调 / 立体声宽度 / 音量自适应补偿 / 响度归一化（10 张网格卡 + 2 张响度独立卡） | `src/services/HyperSoundEngine-v1/ui/effectsPanel.tsx:20-66`（`EFFECT_META` 12 项、`GRID_KEYS`/`ROW_KEYS`） |
| 混响双路由 | 卷积（可导入 IR、去周期化）/ 算法（大厅/房间/板式/弹簧/舞台五种）/ off 三路 | `src/services/HyperSoundEngine-v1/src/types.ts:26,34`、`src/services/HyperSoundEngine-v1/ui/modalsSpatial.tsx:14-19` |
| 低音增强与低音下潜 | 谐波生成（偶次/奇次/ATSR/软饱和四种）+ `lowBoostDb` 低音下潜 −6..+12dB（低通低频带按增益真实混回） | `src/services/HyperSoundEngine-v1/src/dsp/BassEnhancer.ts:19,121-122,165` |
| 内置场景（11） | 流行 / 增强 / 爵士 / 舞曲 / 古典 / 现场 / 录音棚 / 温暖 / 浩渺 / 悠扬舞台 / 深夜低音 | `src/services/HyperSoundEngine-v1/src/engine/ScenePresets.ts:19-31`（`SCENE_IDS`）、`ScenePresets.ts:145-288`（名称） |
| 我的场景 | 用户自建场景，上限 **20**（`MAX_MY_SCENES`） | `src/services/HyperSoundEngine-v1/ui/bridge.ts:31-32` |
| 发布种子 | 官方默认场景层随包分发，开发模式可「写回发布种子」（IPC 限非打包版），revision 递增让旧微调让位 | `src/services/HyperSoundEngine-v1/src/engine/builtinSceneSeed.ts`、`desktop/main.cjs:3565-3569` |
| 均衡器 | 简约 5 段（80/250/1k/4k/12k）+ 专业 10 段 / 20 段 + 级联 Q 补偿 | `src/services/HyperSoundEngine-v1/src/types.ts:28,54,399-403`、`src/services/HyperSoundEngine-v1/ui/eqPanel.tsx`、`eqCurveEditor.tsx` |
| 空间音频（第 15 级） | 5 种模式：`off` / `instant` 一键空间化 / `headLocked` 头锁定环绕 / `world` 世界漫游 / `stage` 舞台影院；合成解析 HRTF + 双插值（最近邻网格 / 实球谐 L=3） | `src/services/HyperSoundEngine-v1/src/spatial/types.ts:18`、`src/services/HyperSoundEngine-v1/ui/pages/SpatialAudioPage.tsx`、`src/services/HyperSoundEngine-v1/src/spatial/analyticHrtf.ts` |
| 扬声器布局 | 头锁定环绕支持 stereo / 5.1 / 5.1.4 / 7.1 / 7.1.4 / 自定义（环形编辑） | `src/services/HyperSoundEngine-v1/src/spatial/types.ts:78`、`src/services/HyperSoundEngine-v1/src/spatial/layouts.ts:19-116` |
| 房间模拟 | 7 种房间预设（studio/hall/stage/church/outdoor/bathroom/corridor，另 `off` 共 8 态）：镜像声源早期反射 + FDN 8 延迟线晚期混响 | `src/services/HyperSoundEngine-v1/src/spatial/types.ts:24` |
| 舞台影院 | 4 种场景预设（stage/cinema/piano/nature）+ 座位（front/middle/back）+ 可附加自定义声源 | `src/services/HyperSoundEngine-v1/src/spatial/types.ts:116,124` |
| 空间音频扩展 | 多普勒 + 遮挡/衍射简化（增益衰减 + 高频低通）、Ambisonics 环境上混、多声道输入映射（binanural/stereo/multichannel） | `src/services/HyperSoundEngine-v1/src/spatial/`、`types.ts:256-258` |
| 响度归一化（引擎内实时） | ITU-R BS.1770 实时测量（整合/短时 LUFS）对齐目标（默认 −14 LUFS，可 −14/−16/−23），切换歌曲音量一致；不依赖任何外部服务 | `src/services/HyperSoundEngine-v1/src/engine/EngineV3.ts:499-522`、`src/services/HyperSoundEngine-v1/src/dsp/LufsMeter.ts`、`ui/modalsLoudness.tsx:148-164` |
| 音量自适应补偿（等响度） | 按系统音量动态补偿低频（最高 +12dB）/高频（最高 +6dB），音量恢复回平 | `src/services/HyperSoundEngine-v1/ui/effectsPanel.tsx:56-58`、`ui/modalsLoudness.tsx:86`、`src/services/HyperSoundEngine-v1/src/dsp/LoudnessComp.ts` |
| 调音室音量滑块 | 经 `loudnessNormalization.externalGainDb`（0–100% → −60..0dB）通道快平滑跟手，独立于场景预设 | `src/services/HyperSoundEngine-v1/attachV3Engine.ts:357`、`EngineV3.ts:524-535` |
| 分析页 | 32 条对数频率轴实时频谱（20Hz–20kHz，2048 点 FFT，100ms 轮询 + EMA）+ LUFS/GR/峰值/真峰值 + 频谱特征 + 听力测试（7 频点 × 5 轮二分逼近听阈） | `src/services/HyperSoundEngine-v1/ui/analysisPanel.tsx:4-5,19,59,148-191` |
| 导出 | 离线 MP3（解码 PCM → `EngineV3.process` 分块 → lamejs 128kbps 下载）；另有 WAV 写入通道（`saveWav`） | `src/services/HyperSoundEngine-v1/attachV3Engine.ts:11,367-419`（`exportV3Mp3`）、`ui/sharePanel.tsx:19-105` |
| 分享串 | 参数序列化为可复制分享文本，含版本/校验/白名单防注入，可导入还原 | `src/services/HyperSoundEngine-v1/src/engine/ShareCodec.ts`、`ui/sharePanel.tsx:27-53` |
| 调音室 UI（9 页导航） | 主页 / 音效场景 / 均衡器 / 空间音效 / 空间音频 / 动态调音 / 分析 / 调音器 / 关于 | `src/services/HyperSoundEngine-v1/ui/V3MixingStudio.tsx:71-80`（`NAV_ITEMS`） |
| 开发者模式 | 关于页开关（`hyperplayer:hse-dev-mode`），启用后可微调内置场景、写回发布种子、场景库 JSON 导出/导入 | `src/services/HyperSoundEngine-v1/ui/sceneStore.ts:20-39`、`src/services/globalSettingsRegistry.ts:947`、`src/components/SettingsPanel.tsx:2956-2980` |
| 引擎适配层 | 统一 `IAudioEngineAdapter` 接口 + 注册表，App.tsx 零分支调用（attach/dispose/setSystemVolume/exportMp3/renderStudio）；当前注册表仅 v3 一项 | `src/services/audio-engine/types.ts:135-196`、`src/services/audio-engine/index.ts`、`engines/v3.ts`、`V3Adapter.tsx` |
| 模块许可红线 | CC BY-NC-ND 4.0 + 专项授权；UI/文档一律称 HSE，代码标识符保持 v3 不动 | `src/services/HyperSoundEngine-v1/LICENSE`、`授权补充说明.md`、`THIRD_PARTY_NOTICES.md` |

---

---

## 域 G：可视化

| 功能 | 说明 | 关键实现 |
|---|---|---|
| 现代模式实时频谱 | 播放页 Canvas 频谱，64 根柱条，45Hz–16kHz 按 mel 刻度映射，逐帧平滑、复用缓冲区避免分配。 | `src/components/ModernAudioVisualizer.tsx:14-16`（`BAR_COUNT=64`）、`:21` |
| 传统模式右栏频谱 | 传统模式「正在播放」卡片的共享分析器 + 独立 rAF 平滑 Canvas。 | `src/components/TraditionalView.tsx:295`、`:431`（`data-testid="traditional-spectrum"`）、开关 `:2061` |
| 共享频谱分析管线 | `AnalyserNode` 以 30fps 采样，输出 **24 段**对数频段，多消费者共享同一快照。 | `src/hooks/useAudioAnalyzer.ts:11`（`ANALYZER_SPECTRUM_BANDS=24`）、`:125` |
| 频谱数学工具 | dB 地板/天花板映射、逐频段 attack/release、频段压缩、对数频段边界。 | `src/utils/spectrum.ts:6-11`、`:27`、`:86`、`:104` |
| 音澜脉冲（beat 律动） | 由分析数据驱动的强度包络，三种模式 `dynamic` / `soft` / `restless`，供频谱层与视觉表面做缩放/亮度脉动。 | `src/hooks/useAudioPulse.ts:4`、`:127`；消费方 `ModernAudioVisualizer.tsx:52` |
| 桌面频谱小组件 | 桌面「音频频谱」小组件，可选 `bars`（能量柱）/`wave`（细波形）两种样式。 | `src/components/DesktopExtraWidgets.tsx:123`、`:563-564` |
| 歌词前奏波形进度条 | 现代歌词模式在前奏/间奏区间画一排随包络起伏的音柱式进度条（主色+辉光+前沿呼吸）。 | `src/components/LyricsDisplay.tsx:1809-1810` |
| HSE 调音室波形/频谱 | 引擎调音室内置波形可视化（属 HSE 模块，UI 侧）。 | `src/services/HyperSoundEngine-v1/ui/components/WaveformVisualizer.tsx`、`ui/pages/HomePage.tsx` |
| Apple 动态封面（HLS） | 命中 Apple 曲目时查询动态封面并在静态封面之上叠 `<video>` 图层播放 HLS（不写回封面字段）。 | `src/hooks/useAppleDynamicCover.ts`、`src/services/appleDynamicCover.ts`、`src/components/AnimatedArtworkCover.tsx` |
| 动态封面消费位置 | 播放页封面组件挂载动态封面层（poster 兜底顺序：预览帧 → 静态封面）。 | `src/components/AlbumCoverPlayer.tsx:3`、`:179` |
| 封面粒子特效（Apple） | Apple 命中歌曲的封面粒子动效组件（Canvas 粒子 + 节拍驱动）。 | `src/components/AppleCoverFx.tsx:7`、`:51` |
| 封面主色提取 | 从封面提取主色/色板驱动播放面配色；模块级缓存 + IndexedDB 封面 blob 缓存，避免 base64 常驻。 | `src/hooks/useColorThief.ts:204`（`useColorThief`）、`:246`（`extractDominantColor`） |
| 桌面强调色可读性 | 由主色派生在深/浅背景上可读的强调色。 | `src/utils/desktopAccentColor.ts:63` |
| PV 歌词主色 | PV 渲染内核自带颜色提取器（背景/风格色）。 | `src/vendor/pv/core/colorExtractor.ts`、`src/vendor/pv/core/engine.ts` |
| 3D 歌单轮播墙 | 桌面模式首页的歌单 3D 轮播（指针驱动、可拖拽、含「最近播放」入口）。 | `src/components/PlaylistCarousel3D.tsx:1`；接入 `src/components/DesktopView.tsx:12`、`:1875` |
| 3D 歌单曲目网格 | 桌面模式歌单详情的 3D 网格视图（懒加载）。 | `src/components/PlaylistGrid3D.tsx:1`；接入 `src/components/DesktopView.tsx:44`、`:2362` |

> 说明：`AppleCoverFx.tsx` 文件存在但当前仓库内**无引用方**（`grep -rn "AppleCoverFx" src/` 仅命中自身），属已实现但未接线的组件。
> 「律动背景（CrossfadeBackground）」与「壁纸歌词」已删除，不在本清单。

---

## 域 H：内容消费

| 功能 | 说明 | 关键实现 |
|---|---|---|
| MV 播放器 | 独立视频播放弹窗：取流、封面详情、全屏/音量/进度、播完行为（下一首/重播/关闭）、列表自动续播；平台限网易云 / QQ。 | `src/components/VideoPlayer.tsx:1`、`:16`；`getMVPlaybackInfo` / `getMVDetail`（`src/services/musicApi.ts`） |
| MV 探索 | MV 分类/地区/类型筛选 + 关键词搜索 + 点播；网易云 7 地区 × 5 类型，QQ 分类/分页。 | `src/components/MVExploreModal.tsx:38-40`、`:62-69`、`:137` |
| B站看歌·自动匹配 | 按「歌名+歌手+时长+歌词」生成候选，加权打分 + 候选类型分类 + 偏好加权，选出最佳 bvid。 | `src/services/bilibiliApi.ts`（`findBestBilibiliMv:2160`、打分权重 `:1374-1577`） |
| B站看歌·双重自动播放门槛 | 门槛随 `strict`/`standard`/`relaxed` 变化，官方且歌手+歌名+时长贴近时降门槛；翻唱/伴奏等负向类型不能仅靠播放量越过门槛。 | `src/services/bilibiliApi.ts:1701`、`:1712-1719` |
| B站看歌·匹配缓存与手动记忆 | 匹配结果按歌缓存（缓存键含设置指纹与评分版本 `v6-audited-identity`），手动选择记忆/黑名单，候选上限 12。 | `src/services/bilibiliApi.ts:1726`、`:1860`、`:2113-2157` |
| B站看歌·CC 字幕当歌词 | 拉取 B站官方/AI 字幕，按偏好（zh-manual/zh-any/any/off）选字幕，清洗 AI 噪音行后作为时间同步字幕条；未验证字幕可缩水升级。 | `src/components/BilibiliMvPlayer.tsx:798-839`、`:898`；`src/services/bilibiliApi.ts`（`getBilibiliSubtitles`、`cleanSubtitleLines:106`、`pickBestSubtitle`） |
| B站看歌·弹幕层 | 拉取弹幕并按设置（字号、透明度、速度、区域等）渲染；失败非阻塞、最多重试 3 次。 | `src/components/DanmakuLayer.tsx:1`；`src/services/bilibiliApi.ts:420`、`DEFAULT_DANMAKU_SETTINGS:607` |
| B站看歌·互动 | 点赞 / 投币 / 收藏 / 发弹幕 / 评论（发、删、点赞）/ 互动状态读取。 | `src/components/BilibiliInteractPanel.tsx`；`src/services/bilibiliApi.ts:439`（发弹幕）、`:507-580`；`server/bilibili-api.mjs:732-921` |
| B站看歌·个人主页 | B站个人中心：皮肤横幅英雄区 + 收藏夹 / 历史 / 投稿 / 关注 四个标签，游标分页、收藏夹网格。 | `src/components/BilibiliProfileModal.tsx:2-7`、`:93-118`；`server/bilibili-api.mjs:1043-1246` |
| B站看歌·登录 | B站扫码登录（生成二维码 → 轮询），登录态与过期天数管理。 | `src/components/BilibiliLoginPanel.tsx`；`src/services/bilibiliApi.ts:644`（`generateBilibiliQr`）、`:348`（`getBilibiliRemainingDays`） |
| B站看歌·设置 | 画质、字幕语言/字号、弹幕、匹配偏好、自动门槛、视频结束行为、关键词模板、强制最高分等。 | `src/services/bilibiliApi.ts:210-256`；`src/components/BilibiliWatchSettingsModal.tsx` |
| MV 背景层（ECHO 风格） | 歌词/播放页背景层：匹配 B站视频静音循环播放，画面时间跟随本地音频时钟（偏差超阈值才 seek）。 | `src/components/BilibiliMvBackground.tsx:2-13`；`src/services/mvBackground.ts`（`computeMvSyncTarget`、`shouldSeekMvVideo`） |
| MV 对齐服务 | 为每个 (歌曲, MV) 计算带置信度的偏移：CC 字幕行时间对齐 + 节拍互相关，置信度不足返回 null；结果持久化。 | `src/services/mvAlignment.ts:3-19`、`:42`（`MIN_ALIGNMENT_CONFIDENCE=0.5`）、`:613`、`:705` |
| B站视频浮层播放器 | 个人主页/收藏/历史点播用全屏浮层：`view → playurl → 流代理` 复用，带 CC 字幕与进度续播，可「设为当前歌曲 MV」。 | `src/components/BilibiliVideoPlayerOverlay.tsx:2-8` |
| B站后端路由 | 搜索/详情/取流/字幕/弹幕/评论/互动/登录/空间/收藏夹/历史/关注等约 30 条路由。 | `server/bilibili-api.mjs:224`（`registerBilibiliRoutes`）、`:226-1246` |
| Apple 探索面板 | 1:1 复刻 music.apple.com：主页 / 新发现 / 广播 / 分类 / 排行榜 / 资料库 六标签，含电台详情抽屉、动态封面。 | `src/components/AppleExplorePanel.tsx:6-17`、`:251-257`；`src/services/appleExploreService.ts:212` |
| Apple 搜索浏览 | Apple 搜索落地页与浏览分类。 | `src/components/AppleSearchBrowse.tsx`；`src/services/appleWebService.ts:869`（`fetchAppleSearchLanding`） |
| Apple 电台直播 | 电台 Now Playing 页：HLS + EME 播放、直播/回放区分、断线按 reconnectKey 判定重连。 | `src/components/AppleRadioNowPlayingPage.tsx:13`、`:68-72`；`src/services/appleRadioReconnect.ts`（`decideAppleRadioFailure`） |
| Apple 音乐视频 | 站内音乐视频弹窗，复用与歌曲/电台相同的 EME license 协议（Widevine EME keyURLs）。 | `src/components/AppleVideoModal.tsx:2-13`；`src/services/appleHlsPlayer.ts:52`（`attachAppleHls`） |
| Apple 播放/目录底层 | 原生 HLS 每个 deck 独立 hls.js/EME/manifest blob；目录/资料库读写、收藏、歌单增删。 | `src/services/appleHlsPlayer.ts:5-34`；`src/services/applePlayback.ts`；`src/services/appleWebService.ts:1262-1433` |

> 「B站看歌」是独立功能，不是第 5 个音乐平台（平台仍为 netease/qq/apple/spotify 四个）。

---

## 域 I：社交与个人中心

| 功能 | 说明 | 关键实现 |
|---|---|---|
| 个人中心 | 我的主页面：创建的/订阅的歌单、最近播放、收藏、听歌排行、云盘、社交、详情等标签页，按平台能力显示。 | `src/components/ProfileView.tsx:99`（`ProfileTab` 联合类型）、`:509` |
| 他人主页 | 从关注/粉丝列表点入他人主页，维护导航栈可逐级返回；查看他人时禁用歌单写操作与平台切换。 | `src/components/ProfileView.tsx:573-589`、`:1455` |
| 关注 / 粉丝 / 社交 | 网易云关注动态与通知消息、QQ 关注用户/粉丝列表，关注/取关/回关按钮。 | `src/components/ProfileView.tsx:518-545`、`:1353-1451` |
| 评论系统 | 热评/最新双视图、分页、点赞、回复展开、删除本人评论、复合资源（歌曲/歌单）。 | `src/components/CommentModal.tsx:62`（`ViewMode='hot'|'latest'`）、`:207`；后端 `local-server.mjs:9099`（网易云）、`:9216`（QQ） |
| 传统模式评论 | 传统模式独立评论弹层（纯展示 + 分页，不实现点赞/回复），走本地 API。 | `src/components/TraditionalComments.tsx:1-3` |
| 歌曲详情 | 封面/标题/歌词/幕后团队、百科、所在歌单、喜欢这首歌的人也爱听、相关歌单、相关博客；Apple 走 web 歌曲页 1:1。 | `src/components/SongDetailModal.tsx:77-155`、`:376` |
| 歌手详情 | 全部歌曲 / MV / 相似歌手 / 详情 / 专辑多标签，分页加载全部专辑与 MV（上限 1000 级 memo 卡片），关注歌手。 | `src/components/ArtistDetailModal.tsx:236`、`:593-619`、`:831` |
| 专辑详情 | 曲目列表、专辑简介、歌手、多平台（Apple 走 iTunes Lookup 免 token）。 | `src/components/AlbumDetailModal.tsx:161`、`:638` |
| 相似歌曲 | 相似歌曲面板，可播放 / 加队列 / 通过上下文菜单操作。 | `src/components/SimilarSongsPanel.tsx:1-8` |
| 歌单管理核心 | 拉取个人歌单、歌单详情、喜欢列表、收藏/取消、增删曲目、创建/删除/更新歌单、上传封面、订阅歌单。 | `src/services/playlistService.ts:431-1101` |
| 歌单 UI 组件 | 歌单面板、歌单详情面板、传统模式歌单详情、创建/编辑/删除弹窗、播放器「加入歌单」弹窗。 | `src/components/PlaylistPanel.tsx`、`PlaylistDetailPanel.tsx`、`TraditionalPlaylistDetail.tsx`、`CreatePlaylistModal.tsx`、`EditPlaylistModal.tsx`、`DeletePlaylistModal.tsx`、`PlaybackAddToPlaylistModal.tsx` |
| 歌单归属判定 | 判断歌单是否特殊（我喜欢/最近播放等）与是否为当前用户所有，决定可执行的管理动作。 | `src/services/playlistOwnership.ts:9`、`:14` |
| 歌单排序编排 | 基于曲目分析做 HAM2 式排序（anchor 锚点 + 序列结果）。 | `src/services/playlistSequencing.ts:124`（`sequenceTracksHam2`） |
| 可加入歌单过滤 | 过滤出可写入的歌单、生成写操作 ID。 | `src/services/addablePlaylists.ts:10-27` |
| 收藏状态服务 | 各平台收藏标识、预取/失效缓存、收藏变更应用。 | `src/services/favoriteStatusService.ts:24-107` |
| 歌单右键菜单 | 歌单上下文菜单与平台能力判定（更新/删除/订阅/分享）。 | `src/components/PlaylistContextMenu.tsx`；`src/components/DesktopView.tsx:1882` |

---

## 域 J：桌面模式与系统集成

| 功能 | 说明 | 关键实现 |
|---|---|---|
| 桌面模式 | 壁纸背景 + 左右两列可拖拽小组件 + 桌面歌词融合层；开启「桌面融合穿透」时空区域鼠标穿透到真实桌面（`setIgnoreMouseEvents(forward)` 实时判定交互区）。 | `src/components/DesktopView.tsx:1467-1503`、`:1576`、`:2035`；`src/components/DesktopWidgetZone.tsx:648` |
| 桌面小组件（25 种） | 时间日期 / 完整日历 / 天气 / 今日进度 / 便签清单 / 备忘录 / 习惯打卡 / 重要日倒数 / 最近播放 / 每日推荐 / 当前播放队列 / 收藏歌曲速览 / 歌单快捷入口 / 听歌统计 / 音乐日历 / 歌手动态 / 平台新歌 / 播放进度 / 热门榜单 / 新专辑 / 歌词摘录 / 音频频谱 / 快捷启动器 / 系统状态 / 音量控制。 | `src/services/desktopCustomization.ts:1-6`（类型联合）、`:39-65`（高度表）；`src/components/DesktopCustomizationEditor.tsx:77-101`（`WIDGET_OPTIONS`） |
| 小组件渲染分组 | 音乐类组件、生产力组件（倒计时/习惯/备忘录/便签）、探索类组件分别渲染。 | `src/components/DesktopExtraWidgets.tsx:392`、`:583`；`src/components/DesktopProductivityWidgets.tsx`；`src/components/DesktopExploreWidgets.tsx`；`src/components/DesktopWidgetZone.tsx:38-39`、`:624` |
| 桌面时间中心 | 时间/日历/世界时钟抽屉：月历、农历、节日纪念日、世界时钟地图（最多 8 城）。 | `src/components/DesktopTimeCenter.tsx:233-367`、`:403-404`；`src/utils/calendarFestivals.ts:52`、`:86` |
| 桌面专注计时 | 桌面「时间日期」组件内的专注计时器（idle/running/paused/ringing）。 | `src/services/desktopFocusTimer.ts:1-105` |
| 自定义壁纸 | 图片/视频壁纸，最多 **12** 个文件（单文件上限 1GB），单张/序列/随机，手动/定时/启动切换。 | `src/services/wallpaperManager.ts:7`、`:32-33`、`:168`、`:367`；`src/components/WallpaperCustomizeModal.tsx` |
| 桌面壁纸（增强） | 桌面模式增强壁纸：单张/轮播/随机 API，随机源 bing/风景/动漫/自定义，顺序/随机循环，暗化/模糊。 | `src/services/desktopWallpaperManager.ts:21-24`、`:110-113`；`src/components/DesktopSettingsModal.tsx:98`、`:619` |
| 桌面播放器小窗 | 独立置顶小窗，`card` / `bar` 双形态（可切换、可拖拽移动、卡片可四角缩放、可展开），默认卡片。 | `src/desktop-player/DesktopPlayerApp.tsx:18`、`:647-679`；`desktop/main.cjs:759`（`DESKTOP_PLAYER_FORMS`）、`:906` |
| 任务栏迷你播控条 | 贴靠任务栏的迷你条（覆盖/普通两种模式、位置/宽度/暗化/隐藏控件可配），定位用 `Shell_TrayWnd`，含歌词 marquee。 | `desktop/main.cjs:1775`（`TASKBAR_WIDGET_DEFAULTS`）、`:1882-1968`；`desktop/taskbar-widget.html:81-86`；`desktop/taskbar-widget-polling.cjs` |
| Thumbar + 任务栏进度 | 缩略图播放/上一首/下一首按钮（带图标缓存），任务栏进度条按播放进度与状态（normal/paused/none）。 | `desktop/main.cjs:1648-1736`（`setThumbarButtons`）、`:1757-1759`（`setProgressBar`） |
| 全局媒体键 | `MediaPlayPause` / `MediaNextTrack` / `MediaPreviousTrack` 全局快捷键，可按设置启停。 | `desktop/main.cjs:710-713`、`:718`；开关透传 `src/services/playbackShortcutSettings.ts:6`（`mediaKeysEnabled`） |
| 窗口状态记忆 | 记住窗口大小/位置/状态（normal/maximized/kiosk）与所在显示器；版本更新后回默认 1400×900。 | `desktop/window-state.cjs:1-24`、`:78`；`desktop/main.cjs:2447`、`:5952` |
| 高刷 / GPU 偏好 | 高刷申请（30–360Hz，`force-frame-rate`）、独显/核显强制、硬件加速开关、GPU 信息与合成状态查询。 | `desktop/main.cjs:77-94`、`:118-131`、`:5759-5800` |
| 天气系统 | 实时/小时（25 点）/十日预报，Open-Meteo + 空气质量独立端点；Apple 场景动画 + 完整/简约两种卡片。 | `src/services/weatherService.ts:904-960`（`fetchWeatherSnapshot`，`api.open-meteo.com`）、`:1035`；`src/components/weatherScene/AppleWeatherScene.tsx`、`AppleWeatherCompactScene.tsx`；`src/components/DesktopWidgetZone.tsx:439` |
| 天气场景模型 | 9 种天气场景（晴/局部多云/阴/雾/毛毛雨/雨/暴雨/雷暴/雪）× 昼夜，共 18 个场景 id，附性能分级。 | `src/weather-debug/scenarios.ts:3-13`；`src/components/weatherScene/weatherSceneModel.ts:27`；`src/components/weatherScene/weatherScenePerformance.ts` |
| 天气地图 | 14 个图层：风/温度/相对湿度/云量/压强/对流能量/海浪/极光/雪深/露点/雷达/PM2.5/森林火灾/太阳净辐照；叶绿素等字段来自 Open-Meteo，地图为预报推算。 | `src/services/weatherMapService.ts:1-15`、`:47-138`；`src/components/WeatherMapExperience.tsx:50-134`；`src/services/weatherMapRendering.ts` |
| 灾害 / 预警 | 台风与地震快照、所在地风险与附近地震、天气预警展示（桌面天气卡片与灾害面板）。 | `src/services/hazardService.ts:121-244`；`src/components/WeatherHazardsPanel.tsx`；`src/components/HazardMapPrimitives.tsx`；`server/hazard-api.mjs:218-242` |
| 月相 | 月相/照度/月升月落/满月倒数/日月天空位置，全屏月相页支持 ±7 天刻度尺拖动；复用 Apple Weather 28 帧月相。 | `src/services/moonPhase.ts:26-229`；`src/components/MoonPhaseExperience.tsx:6-21`；`src/components/weatherScene/appleMoonPhases.ts` |
| 天气定位 | 自动定位/手动行政区选择 + 逆地理编码，城市搜索分级。 | `src/services/weatherService.ts:308-518`；`src/services/locationHierarchy.ts`；`server/location-api.mjs:180` |
| 播放径向菜单 | 长按呼出径向菜单，滚轮/指针选择，最多 **8** 个动作（再听一次/收藏/评论/专辑/歌手/详情/加入歌单/复制信息/相似歌曲），按平台能力过滤，可自定义排序。 | `src/components/PlaybackRadialMenu.tsx:83-199`；`src/components/PlaybackRadialWheel.tsx`；`src/components/PlaybackRadialMenuCustomizeModal.tsx`；`src/services/playbackRadialMenuSettings.ts:4-64`；`src/services/radialLayout.ts` |
| 播放快捷键 | 空格播放/暂停、快进/快退秒数（1–15s，默认 5s）可配，播放页快捷键开关。 | `src/services/playbackShortcutSettings.ts:1-23` |
| 任务栏进度与歌曲联动 | 歌曲变化刷新任务栏进度条，暂停/停止状态映射。 | `desktop/main.cjs:1460` |

---

## 域 K：插件系统

| 功能 | 说明 | 关键实现 |
|---|---|---|
| 插件宿主 / 注册表 | 内置插件（代码注册）与导入插件 manifest 合并视图；列表顺序内置在前、导入按安装时间。 | `src/plugins/registry.ts:16-52`（`registerBuiltinPlugin`、`getAllPluginManifests`、`getPluginRuntime`） |
| 插件类型定义 | `PluginManifest`（元信息、可选 `code`/`notice`/`needsAudio`/`source`）+ `PluginContext`（audio/storage/toast/log）+ `PluginRuntime`（onEnable/onDisable）。 | `src/plugins/types.ts:1-80` |
| 导入插件运行时 | 导入插件代码在 renderer 内以 `new Function` 包装执行（同权限，**非安全沙箱**），代码长度上限 256KB。 | `src/plugins/registry.ts:60-115`；`src/components/PluginImportModal.tsx:49-50` |
| 导入 / 卸载 / 持久化 | manifest 持久化于 localStorage `wf_plugins`，支持安装、卸载、启用状态与「已查看详情/已同意须知」标记。 | `src/services/pluginStore.ts:10`、`:55-150` |
| 使用须知门控 | 未查看详情的插件开关禁用；`requireNotice` 插件首次看详情弹 entry 须知、首次开启弹 consent 须知，确认后永久放行。 | `src/plugins/toggle.ts:1-64`；`src/components/PluginNoticeModal.tsx`；`src/components/PluginDetailModal.tsx:54` |
| 插件中心 UI | App Store 式插件中心主弹窗（卡片 = Logo + 名称 + 简介 + 开关）、详情、导入、通知、快捷键、总览层。 | `src/components/PluginCenterModal.tsx:2`、`:134`；`PluginDetailModal.tsx`；`PluginImportModal.tsx`；`PluginNoticeModal.tsx`；`PluginOverlay.tsx:9-10`；`PluginShortcuts.tsx` |
| 内置插件注册入口 | 插件总览层导入即注册两个内置插件。 | `src/components/PluginOverlay.tsx:9-10` |
| Razer Chroma 插件 | 24 段对数频谱 → 雷蛇整套设备；键盘 12+ 风格（光谱循环/条形/渐变/波浪/径向脉冲/涟漪/呼吸/星光/火焰/雨滴/VU/静态 等）；亮度/灵敏度/衰减/尺寸/平滑/方向/刷新率/色板可调；真实设备识别。 | `src/plugins/ChromaPlugin.ts:9-24`；`src/plugins/clients/ChromaClient.ts`；`src/plugins/clients/chroma/chromaTypes.ts:3-58`；`src/plugins/rgb/rgbSpectrum.ts:3`（`RGB_SPECTRUM_BAND_COUNT=24`） |
| Chroma 设备通道 | 键盘 / 鼠标 / 鼠标垫 / 耳机 / 小键盘 / Chroma Link 六类输出通道分别配置。 | `src/plugins/clients/chroma/chromaTypes.ts:3-9`、`:165`、`:214` |
| Chroma 控制台 / 指南 | 独立灯光工作台（实时预览）与使用指南弹窗。 | `src/components/ChromaConsoleModal.tsx`；`src/components/ChromaGuideModal.tsx` |
| SignalRGB 插件 | HyperPlayer Effect 运行在 SignalRGB 内部，读取其原生 `engine.audio` 驱动全部品牌布局；HyperPlayer 只发播放/节拍/重音/风格/主题等低频语义事件，**不传频谱帧**；12 种原创风格。 | `src/plugins/SignalRgbPlugin.ts:18-36`；`src/plugins/clients/SignalRgbClient.ts` |
| SignalRGB Effect 安装/更新 | 桌面侧管理 Effect 文件安装、版本对比、哈希校验、Pro Local API（`127.0.0.1:16038`）应用/恢复、冲突与重启提示。 | `desktop/signalrgb-effect-manager.cjs:10-145`；`desktop/signalrgb-ipc.cjs`；`src/components/SignalRgbConsoleModal.tsx`；`src/components/SignalRgbGuideModal.tsx` |
| 灯效运行时基建 | RGB 帧调度器、运行时状态、颜色工具、频谱重分箱与动态处理。 | `src/plugins/rgb/RgbFrameScheduler.ts`、`RgbRuntimeStore.ts`、`rgbColor.ts`、`rgbSpectrum.ts`、`rgbTypes.ts` |
| 桌面灯效 IPC | Chroma / SignalRGB 各自 IPC、mock 与设备发现（雷蛇 VID/PID 扫描、应用清单修复）。 | `desktop/chroma-ipc.cjs`、`chroma-mock.cjs`、`chroma-app-list-repair.cjs`、`razer-device-discovery.cjs`、`signalrgb-ipc.cjs`、`signalrgb-mock.cjs` |
| 第三方插件开发文档 | 插件宿主与导入规范、manifest 字段说明。 | `docs/plugin-development.md`（文内 DG_LAB 示例对应的内置插件已移除） |

---

## 域 L：后端与工程

| 项 | 说明 | 关键实现 |
|---|---|---|
| 后端服务主体 | 单文件 Express（约 **11,285** 行），端口 **3001**（仅 127.0.0.1），gzip 压缩、CORS 限 localhost:3000 / file:// / null。 | `local-server.mjs`（`wc -l` = 11285） |
| 路由模块（`server/`，10 个） | `apple-artwork-api` / `bilibili-api` / `byte-lru-cache` / `comment-api-utils` / `hazard-api` / `local-api-health` / `local-service-auth` / `location-api` / `netease-native-explore` / `qrc-decoder`。 | `server/`；注册处 `local-server.mjs:18-27`、`:1073-1076`、`:1690` |
| 平台路由规模 | 网易云约 **92** 条 `/api/netease/*`，QQ 约 **65** 条 `/api/qq/*`，Apple 约 6 条 + `apple-artwork-api` 模块，B站约 30 条。 | `local-server.mjs`、`server/bilibili-api.mjs:224` |
| 本地服务鉴权 | `x-hyperplayer-local-token` 头 + `timingSafeEqual` 常数时间比较；未配置 token 时放行。 | `server/local-service-auth.mjs:3-11` |
| 本地 API 健康/协议 | 服务标识 `hyperplayer-local-api` + 协议版本 1，供探活与协议兼容判定。 | `server/local-api-health.mjs:1-11` |
| Apple bridge | 可选独立进程（端口 **18790**），pywebview + WebView2 承载 music.apple.com MusicKit JS，音频经 MF Widevine 解密；需系统 Python + pywebview，由主进程 spawn，`asarUnpack` 保证 `.py` 可执行。 | `python-apple-bridge/apple_bridge.py`（端口 `:29`，HTTP 协议见文件头注释）；`desktop/main.cjs:6443-6533` |
| Widevine CDM 引导 | 扫描系统 Edge/Chrome 的 Widevine 组件并在 app ready 前注入 `--widevine-cdm-path/version`；找不到时渲染层回退网易云/QQ 载体匹配。 | `desktop/main.cjs:134-165`、`:318-319` |
| SSRF 守卫 | `/api/cover`、`/api/proxy-image` 拒绝内网/回环/链路本地及解析到这些地址的域名；**内部代理链 `proxy-image → cover` 合法**，放行 `localhost:3001`（内层仍校验最终 CDN 目标）。 | `local-server.mjs:1315`、`:1352`（`isPrivateNetworkAddress`）、`:1371-1406`、`:1471`、`:1594` |
| 路径包含校验 | Wallpaper Engine 的 `/preview`、`/media` 在解析后校验 `resolved.startsWith(base + sep)`，越界跳过。 | `local-server.mjs:8960-8976`、`:9008-9024` |
| 外部导航守卫 | 所有窗口（主窗/桌面播放器/歌词窗/任务栏小窗）挂 `guardAgainstExternalNavigation()`（`will-navigate` 拦截外部跳转）；仅 QQ QMK 领取窗允许 `y.qq.com`。 | `desktop/main.cjs:464-465`、`:961`、`:1136`、`:2443`、`:6130`；例外 `:3198-3207` |
| IPC 可信校验 | 按角色（main/taskbarWidget 等）与能力白名单校验 `sender`/`senderFrame` 及其 URL。 | `desktop/trusted-ipc.cjs:1-40`；`desktop/main.cjs:1804-1817` |
| QQ cookie 单事实源 | 全局 `qqMusicCookie` 只在显式登录/设置接口更新；播放类路由用 `resolveRequestCookie()` 只读不写全局；写操作传请求级 cookie 并以 `cookie || qqMusicCookie` 回退。 | `local-server.mjs:78`、`:140`、`:4991`、`:6854-6978` |
| QQ API Key 领取窗 | 独立 session partition `hyperplayer-qq-skill-key`，每次打开前清空避免复用登录态。 | `desktop/main.cjs`（`QMK_OFFICIAL_KEY_URL` / partition） |
| 无损音频下载 | 下载链路与安全/策略校验。 | `desktop/audio-download.cjs`；`test/audio-download-security.test.cjs`、`test/audio-download-policy.test.cjs` |
| 配置与用户数据 | 配置管理、用户数据 profile 隔离。 | `desktop/config-manager.cjs`、`desktop/user-data-profile.cjs` |
| 测试体系（vitest） | **145** 个测试文件（`test/` 100 + HSE `test/` 29 + HSE `ui/` 8 + HSE `src/spatial/test/` 8），环境 node，HSE UI 冒烟按文件头注解启用 jsdom。 | `vitest.config.ts:8-19` |
| 桌面/安全测试脚本 | `npm run test:desktop` = `node --test` 跑 **10** 个用例（apple-url-policy / audio-download ×2 / local-api-health / taskbar-widget-polling / trusted-ipc / update-manager-security / user-data-profile / vmp-status ×2）。 | `package.json:32` |
| 安装器测试 | `npm run test:installer`：生成安装器 UI 资产 + `test/installer.test.cjs`。 | `package.json:35` |
| 灯效插件自测 | `test:chroma`（3 脚本：chroma / 雷蛇设备发现 / 应用清单修复）、`test:signalrgb`（2 脚本：signalrgb / effect）。 | `package.json:29-30` |
| CI 工作流（3 个） | `ci.yml`：push/PR 类型+单测+桌面/安装器/灯效测试+前端构建（ubuntu），tag/手动额外做 Windows 未签名打包验证；`nightly.yml`：每日北京时间 21:00 构建 nightly 预发布（覆盖单一 `nightly` tag，不写 update.json）；`pre-release.yml`：推 `v*` tag 构建预发布（NSIS 安装包 + 热更新 zip，纯语义版本才写 update.json）。 | `.github/workflows/ci.yml`、`.github/workflows/nightly.yml`、`.github/workflows/pre-release.yml` |
| 打包（electron-builder） | 输出 NSIS 安装版 `release/HyperPlayer-<version>-Setup.exe`（每用户安装 `perMachine:false`，不携带用户数据）；`build.files` 白名单含 `desktop/**`、`dist/**`、`server/**`、`shared/**`、`python-apple-bridge/**`、`local-server.mjs`、`package.json`、`logo.png`、`build/**`（清单里的 `THIRD_PARTY_NOTICES.md` 为悬空条目，根目录不存在该文件）。 | `package.json:45-91` |
| Widevine VMP / EVS 签名 | castLabs EVS production streaming 签名链：`vmp:sign:release` → `vmp:verify:release` → `vmp:status:release` 写入 `release/win-unpacked/resources/vmp-status.json`；**签名已打通、账号已配**，实测剩余 **1407 天**（`kind=streaming, status=valid`）。 | `scripts/evs-vmp.cjs`、`scripts/write-vmp-status.cjs`、`desktop/vmp-status.cjs:1-40`；`release/win-unpacked/resources/vmp-status.json` |
| 更新系统 | 检查更新（GitHub manifest + 代理镜像）、后台静默下载热更新包（sha256 校验→staging→`pending-update.json`）、就绪后重启应用；跳过版本、启动自动检测（限频 24h）。 | `src/components/UpdateManager.tsx:2-14`；`desktop/update-manager.cjs:26-338`；`desktop/update-applier.cjs`；`src/services/updateConstants.ts:9-49` |
| 版本号更迭 | 唯一事实源 `package.json.version`；`bump-version.mjs` 更新 package(-lock).json → commit → tag → push，支持 `--no-commit/--no-tag/--no-push/--force/--dry-run`。 | `scripts/bump-version.mjs:1-25`；`package.json:36-40` |
| 版本信息/历史 | 关于页版本信息与版本历史数据。 | `src/services/versionInfo.ts`、`src/services/versionHistory.ts` |

---

---

## 统计

以下数字均为本次实测（Git Bash，仓库根），附验证命令。两半各自独立实测，已去重。

| 项 | 数值 | 验证命令 |
|---|---|---|
| 音乐平台 | 4（netease / qq / apple / spotify） | `grep -n "export type MusicPlatform" src/services/platforms.ts`（第 12 行，4 个成员） |
| 平台能力字段 | 36 | `awk '/export interface PlatformCapabilities/,/^}/' src/services/platforms.ts \| grep -cE "^  [a-zA-Z]+:"` |
| 界面模式 | 4 | `sed -n '3p' src/types/playbackNavigation.ts`（`ViewMode = 'explore' \| 'minimal' \| 'traditional' \| 'desktop'`） |
| 歌词显示模式 | 4 | `grep -n "ALL_LYRIC_MODES" src/App.tsx`（第 297 行，modern/immersive/video/pv） |
| 设置中心标签页 | 4 | `grep -n "setActiveTab\|'account' \| 'advanced' \| 'personalization' \| 'about'" src/components/SettingsPanel.tsx:173` |
| 设置镜像注册表分组 | 8 | `grep -cE "^    id: '" src/services/globalSettingsRegistry.ts` |
| 设置镜像注册表条目 | 46 | `grep -cE "^        id: '" src/services/globalSettingsRegistry.ts` |
| 主题色预设 | 8 | `sed -n '818,828p' src/components/SettingsPanel.tsx`（presetColors 数组） |
| 播放径向菜单动作 | 9（启用上限 8） | `grep -c "{ id: '" src/services/playbackRadialMenuSettings.ts` + `grep -n MAX_PLAYBACK_RADIAL_ACTIONS`（第 17 行 = 8） |
| 首页推荐模块 | 20（最多选用 6） | `grep -cE "^  \{ id: '" src/services/homeModules.ts`（=20）、`grep -n MAX_HOME_MODULES`（=6） |
| 探索模式区块 | 7 | `grep -n "type ExploreSectionId" src/components/ExploreSettingsPanel.tsx`（第 20 行，7 成员） |
| 原生推荐协议路由 | 网易云 12 + QQ 11 | `grep -oE -e "'/api/netease/native/[A-Za-z0-9/_:.-]*'" local-server.mjs server/netease-native-explore.mjs \| sort -u \| wc -l`；`grep -oE -e "'/api/explore/qq/native/..." local-server.mjs \| sort -u \| wc -l` |
| 后端路由数（按前缀） | netease 92 / qq 63 / apple 3 / bilibili 26 / spotify 0 | `grep -oE -e "'/api/<pfx>/[A-Za-z0-9/_:.-]*'" local-server.mjs \| sort -u \| wc -l`（bilibili 用 server/bilibili-api.mjs） |
| HSE 音效项 | 12 | `awk '/export const EFFECT_META/,/^}/' src/services/HyperSoundEngine-v1/ui/effectsPanel.tsx \| grep -cE "^  [a-zA-Z0-9]+: \{"` |
| HSE 内置场景 | 11 | `sed -n '19,31p' src/services/HyperSoundEngine-v1/src/engine/ScenePresets.ts`（`SCENE_IDS`） |
| 我的场景上限 | 20 | `grep -n "MAX_MY_SCENES" src/services/HyperSoundEngine-v1/ui/bridge.ts`（第 32 行） |
| HSE 处理链 | 14 级主链 + 第 15 级空间音频 | `sed -n '486,660p' src/services/HyperSoundEngine-v1/src/engine/EngineV3.ts`（各级序号注释 1–15） |
| EQ 段数 | 5 / 10 / 20 | `grep -n "SIMPLE_EQ_FREQUENCIES\|PRO_EQ_DEFAULT_BANDS\|PRO_EQ_20_BANDS" src/services/HyperSoundEngine-v1/src/types.ts`（第 399/401/403 行） |
| 低音下潜范围 | −6..+12 dB | `grep -n "lowBoostDb" src/services/HyperSoundEngine-v1/src/dsp/BassEnhancer.ts`（第 121 行 clamp −6,12） |
| 空间音频模式 | 5 | `grep -n "export type SpatialMode" src/services/HyperSoundEngine-v1/src/spatial/types.ts`（第 18 行） |
| 房间预设 | 8（off + 7） | `grep -n "export type RoomPreset" src/services/HyperSoundEngine-v1/src/spatial/types.ts`（第 24 行） |
| 舞台影院预设 | 4 | `grep -n "export type StagePreset" src/services/HyperSoundEngine-v1/src/spatial/types.ts`（第 116 行） |
| 调音室导航页 | 9 | `awk '/const NAV_ITEMS/,/^\]/' src/services/HyperSoundEngine-v1/ui/V3MixingStudio.tsx \| grep -c "key:"` |
| 分析页频谱条数 | 32 | `grep -n "const BARS = 32" src/services/HyperSoundEngine-v1/ui/analysisPanel.tsx` |
| MP3 导出码率 | 128 kbps | `grep -n "128" src/services/HyperSoundEngine-v1/attachV3Engine.ts`（`exportV3Mp3`） |
| PV 模板数 | 30（templates 数组）/ 78 个特效注册 | `sed -n '/export const templates/,/^];/p' src/vendor/pv/templates/index.ts \| grep -cE "^  [a-zA-Z]"`；`grep -c "^register(" src/vendor/pv/effects/index.ts` |
| IndexedDB 缓存 store | 4（covers/playlists/lyrics/metadata） | `sed -n '2,7p' src/services/indexedDBCache.ts` |
| 音质档位 | 网易云/QQ/Spotify 6 档；Apple 5 档 | `sed -n '3,16p' src/services/audioQualitySettings.ts` |
| 法律声明章节 | 17 | `grep -c 'h1: "' src/components/legal/LegalAgreement.tsx` |
| 桌面小组件种类 | **25** | `grep -c "^  { type: '" src/components/DesktopCustomizationEditor.tsx` |
| `server/` 路由模块数 | **10** | `ls server/*.mjs server/*.cjs | wc -l` |
| 天气地图图层数 | **14** | `grep -c "id: '" src/services/weatherMapService.ts` |
| 天气场景种类（×昼夜） | **9**（18 场景 id） | `src/weather-debug/scenarios.ts:3-13`；`test/weatherDebugScenarios.test.ts` |
| vitest 测试文件 | **145** = `test/` 100 + HSE `test/` 29 + HSE `ui/` 8 + HSE `src/spatial/test/` 8 | `find test \( -name "*.test.ts" -o -name "*.test.tsx" \) | wc -l` 等 4 条（见 `vitest.config.ts:8-19`） |
| `npm run test:desktop` 用例文件 | **10** | `package.json:32` |
| 后端主文件行数 | **11,285** | `wc -l local-server.mjs` |
| `desktop/` CommonJS 模块 | **22** | `ls desktop/*.cjs | wc -l` |
| 根目录 md | **7** | `ls *.md | wc -l` |
| `docs/` 下 md | **12**（含 adr 3） | `find docs -name "*.md" | wc -l` |
| CI 工作流 | **3** | `ls .github/workflows/*.yml | wc -l` |
| VMP 签名剩余天数 | **1407 天**（`streaming`，`status=valid`） | `cat release/win-unpacked/resources/vmp-status.json` |

> 路由数按「唯一路径字面量」统计（`grep -oE "'/api/<前缀>/[^']*'" | sort -u | wc -l`）。
> 若按注册调用计数会略有出入（如 QQ 唯一路径 63、注册调用 64），本清单取唯一路径值。

---

## 附录 A：不可删改的红线

以下文件均**实测存在**（`find` 验证）。删改会破坏许可合规或授权声明。

| 文件 | 约束 |
|---|---|
| `src/services/HyperSoundEngine-v1/LICENSE` | HSE 引擎许可：**CC BY-NC-ND 4.0**（署名 + 非商业性使用 + 禁止演绎）；作者 IceFire_Icer，© 2026。 |
| `src/services/HyperSoundEngine-v1/授权补充说明.md` | 对 **HyperPlayer 项目**的专项授权：允许在本仓库范围内使用、集成、修改、分发 HSE 代码（含 `public/v3-worklet.js` 打包产物）；授权日 2026-08-18；不改变第三方在项目外仍受 CC BY-NC-ND 约束。 |
| `src/services/HyperSoundEngine-v1/THIRD_PARTY_NOTICES.md` | HSE 第三方来源与许可归属（RBJ EQ Cookbook、DSPFilters MIT、kissfft BSD-3、Freeverb 公有领域、stk MIT、DaisySP MIT 等），须保留版权声明。 |
| 根 `LICENSE` | 仓库主体许可：含**上游母项目 WaveForge（澜音工坊）授权声明**，被授权人 **IceFireIcer**；并含「私有模块许可」适用范围表（无缝衔接、AutoMix、看歌/MV 背景、桌面模式、探索模式、Apple Music 接入）。不得移除/模糊化版权与归属标注。 |
| `NOTICE` | 归属声明：Copyright 2026 IceFireIcer；说明主体许可见根 `LICENSE`、HSE 单独按 CC BY-NC-ND 4.0。 |
| `PRIVATE-LICENSE.md` | 私有模块许可（⚠️ 适用范围表仍列出部分已删模块，以实际存在文件为准）。 |
| `src/services/gapless/LICENSE.private` | 声明 `src/services/gapless/**` 适用根私有模块许可，© 2026 IceFireIcer，未经书面授权禁止复制/移植/改编/再分发。 |
| `src/desktop-player/LICENSE.private` | 声明 `src/desktop-player/**`（桌面独立播放小窗）适用根私有模块许可，同上。 |

> 另注：`package.json` `build.files` 列出了 `THIRD_PARTY_NOTICES.md`，但**仓库根不存在该文件**（悬空条目）；真正的第三方声明在 HSE 模块目录内。

---

## 附录 B：已知限制（每条附依据）

| 限制 | 说明 | 依据 |
|---|---|---|
| 过渡策略单一 | 过渡计划层只产出 **Fixed Crossfade**（+ gapless 无缝 + albumGapless 专辑无缝）；智能节拍匹配/节拍交叉淡化/V2 渲染分支已移除。 | `src/audio/transitionPlanner.ts`（保留 `planTransition`/`planTransitionV2` 导出，仅 fixed-crossfade）；`AGENTS.md` |
| Apple 原生播放依赖 VMP 签名 + 系统 Widevine | Apple 原生 CENC 播放需系统已装 Edge/Chrome 的 Widevine CDM；打包版需 castLabs EVS production streaming VMP 签名。签名有有效期（当前实测剩余 1407 天），到期需重签。 | `desktop/main.cjs:134-165`；`release/win-unpacked/resources/vmp-status.json`；`docs/APPLE_MUSIC_ELECTRON_L3_VMP_TEST_REPORT_2026-08-31.md` |
| Apple bridge 可选且需外部运行时 | 18790 bridge 需系统 **Python + pywebview**；未安装时启动跳过，Apple 播放面降级。**无内置 Python 运行时**。 | `python-apple-bridge/apple_bridge.py` 文件头；`desktop/main.cjs:6454-6533` |
| 音效引擎单一 | 注册表仅 v3（HSE）一项，无版本切换 UI；v1/v2 已删除。 | `src/services/audio-engine/index.ts`；`src/services/audioEngineVersion.ts` |
| 法律条款为中文单语 | 法律协议为**中文单语**（17 章，其中第六条免责声明 6 段），多语言 i18n 框架已移除；如需多语言须重新引入。 | `src/components/legal/LegalAgreement.tsx`（`grep -c 'h1: "'` = 17）；`AGENTS.md` |
| 天气依赖外部服务与定位 | 天气/空气质量来自 `api.open-meteo.com` / `air-quality-api.open-meteo.com`；自动定位需系统定位或用例手动行政区；无网络/无定位时等待或失败。灾害数据另依赖后端 `/api/hazards/*`。 | `src/services/weatherService.ts:931-960`；`server/hazard-api.mjs:218-242` |
| 天气地图为预报推算 | 地图图层由逐小时预报网格采样/推算（雷达由降水强度换算），非实测雷达回波；极光/火险等为估计/派生值。 | `src/services/weatherMapService.ts:43`（`source: 'forecast'|'air-quality'|'marine'|'derived'|'estimate'`）、`:119-137` |
| 导入插件非安全沙箱 | 导入插件代码在 renderer 内与主应用同权限执行（`new Function`），仅做 manifest 校验与代码长度上限，**不构成安全沙箱**。 | `src/plugins/registry.ts:4-6`、`:60-115`；`src/components/PluginImportModal.tsx:49` |
| 看歌质量受平台限制 | B站自动匹配对现场版/翻唱/完全对不上的视频给低置信度、走人工确认；未能对齐时自由循环。CC 字幕依赖视频是否有字幕。 | `src/services/bilibiliApi.ts:1701-1719`；`src/services/mvAlignment.ts:3-19、42` |
| 传统模式评论弱交互 | 传统模式评论弹层为纯展示 + 分页，不实现点赞/回复交互（简约模式 `CommentModal` 才支持）。 | `src/components/TraditionalComments.tsx:1-3` |
| MV 播放器限平台 | 独立 MV 播放器仅支持网易云 / QQ；B站为独立链路，Apple 音乐视频走 Apple 弹窗。 | `src/components/VideoPlayer.tsx:16`；`src/components/MVExploreModal.tsx:21` |
| 打包容错条目 | `build.files` 含不存在的 `THIRD_PARTY_NOTICES.md`（悬空）；`base:'./'`、`logo.png`/`build/**`、`electronDist` 三条为打包硬约束，破坏会导致黑屏/缺资源。 | `package.json:45-63`；`AGENTS.md`「打包三大约束」 |
| 桌面融合穿透依赖 Windows | 空区域穿透用 `setIgnoreMouseEvents(forward)`，仅 Electron/Windows 生效；纯 Web 页面无法测试。 | `src/components/DesktopView.tsx:1467-1503` |
| `AppleCoverFx` 未接线 | 封面粒子特效组件存在但仓库内无引用方（当前不生效）。 | `grep -rn "AppleCoverFx" src/` 仅命中自身 |

---

## 附录 C：文档索引（现存，实测 `ls`/`find`）

**根目录 md（7 个）**

| 文档 | 用途 |
|---|---|
| `AGENTS.md` | 工程约定与边界：命令、端口、镜像机制、打包三大约束、后端安全不变量、目录职责（开发前必读）。 |
| `README.md` | 功能地图与快速开始（音源 / 无缝 / 歌词模式 / 音效 / 空间音频 / 桌面模式）。 |
| `HANDOVER.md` | 交接文档（⚠️ 内容早于减配，涉及已删功能的段落失效，以 `AGENTS.md` 与源码为准）。 |
| `CONTEXT.md` | 音效域词汇表（效果 / 场景方案 / 自定义状态 / 频响补偿等术语定义）。 |
| `PRIVATE-LICENSE.md` | 私有模块许可声明（⚠️ 范围表含已删模块）。 |
| `DEBUG_PAGES.md` | 独立调试页注册表（Weather Lab；新增独立调试页前必读）。 |
| `changelog.md` | 版本更新日志（**由 release 工作流在 CI 内产出，不入库**；当前版本 1.0.0）。 |

**`docs/` 下 md（12 个，含 `docs/adr/` 3 个）**

| 文档 | 用途 |
|---|---|
| `docs/adr/0001-叠加效果模型与快照式场景方案.md` | ADR：音效叠加效果模型与快照式场景方案（历史决策）。 |
| `docs/adr/0002-频响补偿与均衡器互斥.md` | ADR：频响补偿与均衡器互斥关系。 |
| `docs/adr/0003-导出离线链与实时链共享构建.md` | ADR：导出离线链与实时链共享构建。 |
| `docs/apple-native-playbook-20260830.md` | Apple 原生音源调查结论与后续路线。 |
| `docs/apple-webview2-handover.md` | Apple 原生源方案：Electron L3 主路径 + WebView2 兼容兜底。 |
| `docs/APPLE_MUSIC_ELECTRON_L3_VMP_TEST_REPORT_2026-08-31.md` | Apple Music Electron 原生 CENC / VMP 严格验证报告（含签名剩余天数记录）。 |
| `docs/netease-native-recommend-protocol-2026-09-06.md` | 网易云手机客户端推荐页逆向记录（原生推荐流协议）。 |
| `docs/qq-native-recommend-protocol-2026-09-06.md` | QQ 音乐推荐页原生协议调查（两条手机客户端数据源）。 |
| `docs/qq-recommend-page-scan-2026-09-06.md` | QQ 音乐推荐页脱敏扫描报告。 |
| `docs/plugin-development.md` | 第三方插件开发文档（宿主与导入规范；DG_LAB 内置插件已移除）。 |
| `docs/歌词对比-LyricsBlossom.md` | 「现代」歌词模式 × LyricsBlossom(Apple Music) 逐字动画逆向对比分析。 |
| `docs/功能清单3.0.md` | **本清单**（12 个域 A–L + 统计 + 附录 A–D）。 |

> `src/services/HyperSoundEngine-v1/docs/` 内有 HSE 模块文档（融合指南 / UI 指南 / 算法文档 / 架构书等），属模块内文档，不列入本索引。

---

---

## 附录 D：存疑事项复核

调研中标注的存疑点逐条复核如下。

| # | 存疑点 | 复核结果 |
|---|---|---|
| 1 | **法律协议弹窗无调用点** | ✅ **确认为真实缺陷并已修复**。`LegalAgreement.tsx`（17 章条款）此前无任何渲染入口，而 `PlatformLoginNotice.tsx:105` 仍引导用户去「设置 → 关于 → 法律声明/用户协议」。已在 `SettingsPanel.tsx` 关于页补回入口与弹窗渲染（`showLegalModal`），引导文案随之重新成立。 |
| 2 | **「无损下载」找不到用户入口** | ✅ 确认。`desktop/audio-download.cjs` 实为分析用缓存下载；`saveWav` 仅有类型声明、无调用方。**用户可导出音频的唯一入口是 HSE 调音室导出 MP3**（128kbps）。本清单未沿用旧文档的「无损下载」。 |
| 3 | **HSE 文档与代码表述冲突** | ✅ 以代码为准：`EngineV3.ts` 的结构是 **14 级主链 + 第 15 级空间音频**（内联）。模块内旧文档 `FUSION_GUIDE.md:248` 的表述未同步。 |
| 4 | **Spotify 无本地后端路由** | ✅ 确认。`/api/spotify*` 计数为 0，Spotify 由前端直连官方 API（`spotifyService.ts`），与其余三平台的「后端代理」模式不同。 |
| 5 | **PV 特效数量** | ✅ 代码实测 78 个特效注册 / 30 个模板；`README.md` 自述「54+ 特效」为旧数。本清单采用实测值。 |
| 6 | **`AppleCoverFx.tsx` 无引用方** | ✅ 确认。该文件存在，但仓库内除自身外无引用点，属**未接线组件**；已在域 G 标注，避免误判为生效功能。 |
| 7 | **桌面小组件数量** | ✅ 已补齐：**25 种**（见域 J 与统计表）。 |
| 8 | **路由计数口径不一致** | ✅ 已统一为「唯一路径字面量」，并在统计表下方注明口径差异。 |
